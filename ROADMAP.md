# GOBS Golf — Feature Roadmap

*Last updated: 2026-05-20 — beta feedback sprint for Thursday May 21 round shipped: #4 (player-driven team formation), #2 (blind-draw par display fix), #3 (leaderboard per-team THRU N / FINAL caption). D1.11 + H1 (admin edit mode + /thomas-admin → /admin rename) also shipped same day. 344/344 tests green across 37 files.*

---

## How to use this file

**Statuses**

- ✅ **Shipped** — live and working
- 🔨 **In Progress** — actively being built
- 📋 **Ready to Build** — spec is locked, can start anytime
- ❓ **Blocked** — needs info from Dad or a decision

**Phases are ordered by dependency.** Phase A unblocks Phase B, etc. Items within a phase can usually be built in parallel.

**Active priority order (post-May 20, 2026):** Phase H.2 → Phase H.2.5 → Phase E → Phase F.1 → Phase G onward. Phase 0.5, Phase A.1, Phase A.2, Phase C, and Phase D.1 are all complete. Phases below are listed in dependency order; next-up work follows this priority list. Prior priority orders: post-May 18 was H.2 → E → F.1 → G onward; post-May 13 was D.1 → H.2 → C PR 3 → E onward; pre-May 13 was 0.5 → A.1 → D.1 → H.2 → C PR 3 → E onward.

**One source of truth.** The companion document `GOBS_Game_Rules_v1.docx` defines all scoring logic. This roadmap covers what to build; that document covers how scoring works.

---

## Phase 0.5 — Live-Test Critical Fixes ✅

*Surfaced from the May 8 first-live-course test. Both items blocked real-round use; both now resolved by 2026-05-13.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| LT1 | Course Handicap display mismatch on scorecard | ✅ (2026-05-09) | Self-healing recompute shipped at scorecard load (`a779ced`). On every scorecard mount, if the stored `round_players.course_handicap` doesn't match `computeCourseHandicap(handicap_index, slope, rating, par)`, the corrected value is set into local state immediately and fire-and-forget written back to the row. Single source of truth in the scorecard file — row CH display, dots logic, and engine all read the corrected value. Downstream consumers (summary, leaderboard, season) pick up the corrected DB value on their next load. **Live-round verification still pending** — code path is shipped, but the back-to-back live round with Dad confirming a corrected CH on his phone hasn't happened yet. Next-session priority. |
| LT2 | Scores reverting to par on hole navigation | ✅ (2026-05-13) | Originally suspected to be an A6 first-tap-lands-on-par regression. 2026-05-10 triage on `lt2-repro` ran 3 variants — all clean, score-overwrite and dual-bug-cumulative+persistence both ruled out for those scenarios. 2026-05-13 Track B investigation found the real mechanism: silent write failures (Supabase upsert error swallowed in the original `setScore`) combined with `load()` rehydrating from DB on remount → optimistic state overwritten with stale DB. **Closed by Option 3 / Bug 1 work** — see the new "Bug 1 Resolution" section below. Both the failure path (queue retains items + retries) and the surface (Phase D end-of-round dialog + Phase E stale-failure prompt) are now wired. `lt2-repro` branch and its instrumentation are no longer needed; safe to delete once Jonathan has used it as reference. |

**Phase 0.5 exit criteria:** Both code paths shipped and merged to master. LT1's self-heal is structurally correct (verified by code review and existing handicap-engine unit tests) but never confirmed under live-round conditions — that confirmation moves to next-session priority #1. LT2 / Bug 1 is closed by Option 3 (Phases A–E) with 22 new component/integration tests including a Sequence F that explicitly demonstrates the fix.

---

## Bug 1 Resolution — Option 3 Durable Write Queue ✅ (shipped 2026-05-13)

*Bug 1 was originally tracked as LT2 in Phase 0.5. The root cause turned out to be silent write failures, not a hole-nav regression. Option 3 closes it durably with a localStorage-backed write queue.*

**Design doc:** [`docs/option-3-write-queue-design.md`](./docs/option-3-write-queue-design.md). Captures all 14 locked decisions (D1–D14): storage layer, namespace, queue item shape, collapsing rules, DB conflict resolution, retry / backoff policy, failure classification, UI flow (invisible during play; reconciliation on End Round; on-open stale-failure prompt), drain triggers, multi-tab handling, pre-drain on pagehide, quota handling, Sentry instrumentation.

**What shipped:**

| Phase | Commit | What it does |
| --- | --- | --- |
| A | `08ecdce` | `setScore` switched from SELECT-then-INSERT/UPDATE to a single `.upsert(..., { onConflict: "round_player_id,hole_number" })`. One round-trip, idempotent under retry. No new migration — Phase 2's audit had missed that `scores_round_player_id_hole_number_key` already existed (predates `supabase/migrations/`; `list_tables` doesn't surface unique constraints). |
| B | `116801f` | New `src/lib/writeQueue/` module: `WriteQueue` class + `localStorage`-backed storage + backoff helper. Scaffolding only, not imported by anything yet. Full unit test coverage. |
| C | `cc0d773` | Wires the queue into the scorecard. `setScore` enqueues instead of awaiting. `load()` drains the queue *before* rehydrating, then overlays any still-pending or in-flight items onto `scoreMap`. Production `instance.ts` singleton wraps `@sentry/nextjs` as the reporter. |
| D | `76e1a8b` | End-Round reconciliation flow. Tap → spinner → hail-mary drain → 30s timeout with "Skip and finish" at 15s → first-attempt dialog (`Retry sync` / `Skip and finish`) → second-attempt dialog (`Try Again` / `Copy details` / `Finish anyway`). New `markAsTerminal(ids, reason)` API. |
| E | `668da1e` | Homepage on-mount stale-failure prompt. `Retry` / `View details` / `Forget`. `sessionStorage`-scoped dismissal. Multi-round-grouped clipboard format. |

**Failure mechanisms closed:**

| Mechanism | Closed by |
| --- | --- |
| Write failures mid-flight | Phase B retry loop with exponential backoff (1s → 120s, then 120s forever during round) |
| Tab eviction during write | Phase C localStorage persistence + Phase C `load()` overlay |
| Offline-on-remount | Phase C overlay reapplies pending queue items onto `scoreMap` before render |
| End-of-round recovery | Phase D dialog surfaces stuck items before the user leaves the scorecard |
| Multi-session recovery | Phase E prompt surfaces terminal items left over from prior session |

**Code locations** (one-paragraph orientation for future extenders):

- `src/lib/writeQueue/` — the queue itself. `WriteQueue.ts` is the class. `instance.ts` is the production singleton that wraps Supabase upsert + `@sentry/nextjs`. `storage.ts` is the localStorage adapter with in-memory fallback and quota-eviction. `backoff.ts` exports the D7 schedule constants. `types.ts` is the public type surface. `index.ts` re-exports everything.
- `src/components/scorecard/` — the dialogs. `ReconciliationDialog.tsx` is Phase D's End-Round dialog (both variants). `FinishingSpinner.tsx` is the spinner. `StaleFailureDialog.tsx` is Phase E's homepage prompt. `stuckItemsClipboard.ts` has both formatters (single-round for Phase D, multi-round-grouped for Phase E).
- `src/app/round/[id]/scorecard/page.tsx` — Phase C wiring (setScore enqueues, load drains+overlays) + Phase D handlers (startEndRoundFlow, runHailMaryWithTimeout, handleRetrySync, handleSkipAndFinalize, handleCopyDetails, finalizeRound).
- `src/app/page.tsx` — Phase E homepage mount-check + handlers.
- `tests/lib/writeQueue/` — unit tests for the queue module.
- `tests/components/scorecard-bug-repro.test.tsx` — Phase 3 + Phase C bug-repro sequences A–G.
- `tests/components/end-round-flow.test.tsx` — Phase D integration tests.
- `tests/components/stale-failure-homepage.test.tsx` — Phase E integration tests.

**How to extend:**

- **New queue write kinds:** the `QueueItem.kind` discriminator is set up to support more than `"score_upsert"`. Add a new kind, extend `WriterFn` to dispatch on it, and Phase A's upsert call moves out of `instance.ts`'s closure into a per-kind dispatcher.
- **New drain triggers:** add an event listener inside `WriteQueue.start()` and call `void this.drain()`. The existing triggers (online / visibilitychange / pageshow / pagehide / 30s backstop / enqueue) cover the practical cases.
- **New Sentry events:** add a `captureMessage` call inside `WriteQueue` with a descriptive event name and `reason` discriminator. Phase E's `"user_forget_stale"` is the model.
- **Multi-tab live sync** (deferred Phase F per the design doc): would require Supabase Realtime subscriptions. Out of scope for Bug 1 closure; revisit only if admin laptop + player phone editing the same scorecard concurrently becomes a real workflow.

**Sentry instrumentation surface** (per D14): `writeQueue.terminal_failure` (with `reason` discriminator: `classified_terminal`, `stuck_too_long`, `end_round_timeout`, `end_round_retry_timeout`, `stale_failure_retry_timeout`), `writeQueue.evicted` (storage quota), `writeQueue.writer_threw` / `writeQueue.storage_save_failed` / `writeQueue.listener_threw` (drain-loop crashes), `writeQueue.size_threshold_exceeded` (queue ≥100 items), and `writeQueue.forget` (with `reason: "user_forget_stale"` for Phase E forget taps; `reason: "forget"` for any other future caller). Routine ops (every enqueue / drain / retry) are intentionally NOT logged to avoid drowning the signal.

**Test surface added:** 22 tests in `tests/components/StaleFailureDialog.test.tsx` + `tests/components/stale-failure-homepage.test.tsx` + multi-round formatter (Phase E); 20 in `tests/components/end-round-flow.test.tsx` + `ReconciliationDialog.test.tsx` + clipboard formatter (Phase D); 27 in `tests/lib/writeQueue/{backoff,storage,WriteQueue}.test.ts` (Phase B). Plus the Phase 3 component tests carrying Sequences A–G in `scorecard-bug-repro.test.tsx`. **Total master: 240/240 passing.**

---

## Phase A — Bug Fixes & Scorecard UI Cleanup

*Low-risk, high-value. Do this first to give the league a better-feeling app while bigger work is being designed.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| A1 | Remove duplicate team gross/net at bottom of scorecard | ✅ | Keep only the top scorecard pill. Drop gross from the top pill entirely; show only net. |
| A2 | Score format: big number + small `(+/−N)` | ✅ | Big number = team net score, parenthetical delta to the right |
| A3 | Remove duplicate "Hole 1" indicator | ✅ | Hole label appears once, not twice |
| A4 | Rename "CH" / "HC" / "HCP" → "Strokes" | ✅ | Apply across scorecard, player tab, profile, admin players tab |
| A5 | Show handicap strokes as dots | ✅ | One dot per stroke that player gets on current hole, above +/− buttons. No number, dots only. |
| A6 | Default scorecard value = dash, anchored to par | ✅ | Display starts as `—`. First +/− tap lands on par for the hole; subsequent taps increment/decrement normally. Database stores nothing until tap. |
| A7 | Bug: admin-created scorecards not showing player names | ✅ | Currently in production. Fix in next code push. |
| A8 | Keep gross score on round summary + history detail pages | ✅ | Drop from scorecard pill only; preserve elsewhere for "I'm curious" lookups |
| A9 | Hide BALL / Tied badges + override hint in Stableford-family formats | ✅ | Best-N "BALL 1"/"BALL 2"/"Tied" pills, the tied-for-Ball banner, and the "tap a card to override which balls count" footer only show in best-N formats (2-Ball, 3-Ball; Best Ball is best-1 and gets a single-ball UX in Phase A.1 / A1.4). In Stableford Standard / GOBS Stableford every player's score contributes, so those affordances are misleading and now hidden. (Stableford Modified / GOBS House dropped 2026-05-09; gating logic in code rewires under A1.3.) |

**Phase A exit criteria:** Scorecard reads cleaner, no duplicate displays, "Strokes" terminology consistent everywhere.

---

## Phase A.1 — Pre-Monday Stableford & Format Cleanup

*Targeted at the next round (Monday May 11). Numbering re-aligned 2026-05-10 to match the PR chunk plan that bundled rename + new tables + editable UI together. Ships after Phase 0.5. Two scorecard polish items (A1.6/A1.7) deferred to a follow-up PR — they sit on the live score-entry surface that's paused while LT2 is reproduced.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| A1.1 | Rename `stableford_modified` → `gobs_stableford` + new league point tables | ✅ | Engine enum rename, dispatcher branch rename, FORMAT_LABELS / FORMAT_ORDER / DEFAULT_FORMAT_CONFIG, scorecard/summary gates, FormatPicker STABLEFORD_FORMATS, leaderboard rank STABLEFORD_FORMATS. New locked point tables (see Decisions Locked). `getStablefordPoints` confirmed correct (delta ≥ +2 collapses to DB+ bucket — `engine.ts` final return). Migration `003_phase_a1_format_set_rebalance.sql` updates the CHECK constraint and renames any rows (none in prod). |
| A1.2 | Add Best Ball as a new format | ✅ | New enum value `best_ball`. Engine dispatcher → `computeBestNHole` with N=1 via `defaultBestN("best_ball") === 1`. Strict best-1 net per hole regardless of team size. Net-only — net/gross toggle disabled in FormatPicker with caption "Best Ball is always net" and a `useEffect` that force-flips local state to "net" any time `best_ball` is selected (defense against stale gross choice). Best Ball IS a best-N family format → override holes apply normally (not a no-op). `isBestNFormat` in scorecard now includes `best_ball`. New unit tests in `engine-best-ball.test.ts` + new `snapshot-b6` script. |
| A1.3 | Move format picker into admin Round Setup tab + decouple from scorecard unlock | ✅ | Entry point is the new "Today's Format" button on the Round Setup tab (sibling to "Edit Teams"). Either button auto-creates a round shell for today if none exists — `ensureRoundShell()` in `RoundSetup.tsx` is the shared helper. Format-setting and team-building are fully independent flows from the admin's perspective. Players starting a scorecard from `/` also auto-create a shell via `/round/new`, and that scorecard locks via `ScorecardLockNotice` until admin picks a format. No format UI appears on any player-facing surface. `FormatNotSetBanner` deleted in the 2026-05-10 flow pivot (replaced by the Today's Format button); `FormatChip` kept (still used as the read-only chip on the player scorecard header). |
| A1.4 | Drop GOBS House | ✅ | Removed `gobs_house` from `Format` enum, `GOBS_HOUSE_POINTS` constant, dispatcher branch, `FORMAT_ORDER`, `FORMAT_LABELS`, `DEFAULT_FORMAT_CONFIG`, FormatPicker `STABLEFORD_FORMATS`, leaderboard rank `STABLEFORD_FORMATS`, summary Stableford gate, all tests (engine-stableford, engine-overrides, rank, helpers, copy), and snapshots b4 Part 4 + b5 Part 3. Migration drops it from the CHECK constraint. Pre-migration check confirmed zero `gobs_house` rounds in production (2026-05-10). |
| A1.5 | Editable point-values UI for GOBS Stableford | ✅ | New section in `FormatPicker.tsx` that renders only when `selectedFormat === "gobs_stableford"`. Six rows — Albatross / Eagle / Birdie / Par / Bogey / Double Bogey or worse — each with a numeric input prefixed by the GOBS default. Values clamped to [−10, +10] to prevent typo blow-ups. "Reset to defaults" link below the rows. Stableford Standard section does NOT render — Standard's table is locked at the constant. Saved values land in `format_config.point_values`, which the engine already reads via `mergePointTable(GOBS_STABLEFORD_POINTS, …)`. Mid-round edits route through the existing FormatPicker DangerModal (already wired for any `format_config` change post-lock). Resolves TD7. |
| A1.6 | F9 / B9 / Total on scorecard team-net pill | ✅ | Cumulative-net row below the big delta on the team-net pill at 13px, labels at opacity 0.65, values weight 500 on the navy pill. Helper `getTeamNetDeltaForHoles(holes)` derives each leg from `buildRoundInput("net").perHole` (returns `teamScoreSubtotal − teamParSubtotal` for best-N; Stableford collapses to absolute points via C3). `null` per leg when no holes in range have scored → "—". **Update 2026-05-17 (polish P1):** "Tot" segment removed — redundant with the headline team-net big number. Row is now `F9 [val] · B9 [val]`. |
| A1.7 | Tap player row → expand hole-by-hole on scorecard | ✅ | Tap player name / left section OR chevron-down on the right to reveal that player's hole-by-hole. Multi-expand (others don't auto-collapse). Two stacked grids (1–9 with F9 subtotal cell, 10–18 with B9 subtotal cell), thin divider; 3 rows per grid (hole #, par, gross). Current hole highlighted on header + score cells (#dbeafe), unplayed in #94a3b8 "—". Bottom-right "Total N" line. +/− buttons stay independent of the expand tap target. Grid extracted to `src/components/scorecard/PlayerHoleGrid.tsx` as a reusable component (props: `scores`, `par`, `currentHoleIndex?`, `showRunningTotal?`). Remove-from-team affordance, formerly on the player-name tap, moved into the expanded panel as a small underlined link. **Update 2026-05-17 (polish P2 + P3):** the green-text birdie color was replaced with traditional notation marks (concentric circles for under-par, squares for over-par; tiered 1/2/3 deep up to triple), and the three rows received a hierarchy refresh — navy/500 hole numbers, italic/muted par, semibold/primary gross. |

**Phase A.1 exit criteria:** GOBS Stableford selectable with editable per-round point values. Stableford Standard scoring correctly under new locked table. Best Ball selectable, net-locked. Format pickable from Round Setup tab (already). GOBS House fully removed from codebase, enum, UI, tests, and snapshots. All snapshots clean; `tsc --noEmit` clean. A1.6 ✅ shipped 2026-05-17; A1.7 ✅ shipped 2026-05-17. **Phase A.1 fully closed.**

### Phase A.1 polish — 2026-05-17 (post-A1.7)

Three small commits to refine the surfaces introduced by A1.6 + A1.7. All three are scorecard-only; leaderboard, summary, blind draw untouched.

| # | Change | Commit |
| --- | --- | --- |
| P1 | **Drop "Tot" from team-pill F9/B9/Tot row** in `src/app/round/[id]/scorecard/page.tsx`. The headline team-net delta already is the total by construction — `Tot` was redundant. Row becomes `F9 [val] · B9 [val]`. `getTeamNetDeltaForHoles(ALL_HOLES)` call + the unused `ALL_HOLES` constant dropped. Empty-state `"F9 — · B9 —"` preserved. | `370c7df` |
| P2 | **Add traditional notation marks** to score cells in `src/components/scorecard/PlayerHoleGrid.tsx`. Shape replaces the prior green-text birdie treatment, so the over/under-par signal is uniform across all cells. Per-delta: ≤−3 triple circle (incl. par-4/5 ace), −2 double circle, −1 single circle, 0 bare number, +1 single square, +2 double square, ≥+3 triple square. New internal `<ScoreMark>` subcomponent wraps the number in nested fixed-size divs (1px border, `currentColor`) with `borderRadius: 50%` for circles or `0` for squares. Tiered sizing 22 / 26+20 / 28+22+18 per spec. Score cell `min-height` bumped 22 → 32px so triple notation has breathing room above the F9/B9 divider. Notation renders on top of the current-hole highlight via `currentColor`. 8 new tests cover each tier + cap. `COLOR_BIRDIE` dropped. | `00a54c3` |
| P3 | **Three-row visual hierarchy** in `PlayerHoleGrid.tsx`: hole-number row + F9/B9 label now navy primary `#042C53` at weight 500 (was muted/700); par row picks up `fontStyle: italic` (color/weight unchanged); gross row + subtotal use weight 600 primary text (was 700). Hole-number row no longer special-cases the current-hole color — the light-blue background still marks it. Reading order is now header → reference → data without labels. | `23d7379` |

Behavior preserved across all three changes: tap-to-expand, multi-expand, chevron, +/− independence, current-hole highlight, Remove-from-team link inside the expand panel, F9/B9/Total subtotals, `showRunningTotal` prop semantics. `PlayerHoleGrid` is still the shared component consumed by both `RoundResultsView` (summary + leaderboard) and the scorecard — these polish changes propagate everywhere uniformly, which is the intended consequence of the A1.7 extraction.

---

## Phase A.2 — Player-Driven Team Formation ✅ (shipped 2026-05-20)

*Beta feedback #4. Players now form their own teams from the homepage rather than relying entirely on admin drag-and-drop. Ships as 5 commits (+ 2 follow-ups) on 2026-05-20 ahead of the Thursday May 21 round.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| A2.1 | Lift `ensureRoundShell` to shared lib | ✅ (`1392c2f`-1) | `src/lib/round/ensureRoundShell.ts` extracted from `RoundSetup.tsx`. Consumed by homepage and scorecard page; `/round/new` inline pattern left (TD19 — tangled with team-number resolution). |
| A2.2 | `smartJoin` pure logic | ✅ (`1392c2f`-2) | `src/lib/teamFormation/smartJoin.ts`. Three branches: (a) player taps themselves with no team → checks played-with matrix + HI balance, returns optimal `{joinTeamNumber}` or `{formNewTeam}`; (b) player taps teammate → adds them to that team; (c) admin override. Pure function, 9 unit tests. `SmartJoinResult` and `RoundPlayer` types exported from this module. |
| A2.3 | `PlayerPickerSheet` component | ✅ (`1392c2f`-3) | `src/components/teamFormation/PlayerPickerSheet.tsx`. Two modes: "Join or start team" (self-selection, shows team options with played-with context) and "Add to my team" (pick a teammate from unassigned pool). Mobile bottom sheet, desktop modal. |
| A2.4 | Homepage integration | ✅ (`187f9ed`) | `src/app/page.tsx` today's-teams section wired to picker. Per-player write queue ported from `RoundSetup.tsx` (see Locked patterns in CLAUDE.md). `JoinTeamConfirmModal` (single confirm tap) and `MixedTeamsErrorModal` (when smart-join can't find a clean assignment). Duplicate today's-teams section deduplicated in follow-up commit; `TodaysTeamsList` component deleted; "Form a new team" CTA folded into existing card. |
| A2.5 | Manage Team sheet on scorecard | ✅ (`c7d4694`) | "Manage Team" button appears on scorecard header (pre-scoring only — hidden once any score is entered). Opens `ManageTeamSheet` for adding/removing players mid-session. Per-player write queue pattern used here too. |

**Phase A.2 exit criteria:** Players can join a team, form a new team, or add a teammate from the homepage without admin intervention. Admin retains full override via existing Edit Teams flow. Manage Team visible on scorecard before first score. All locked decisions documented in the Decisions Locked section below. 344/344 tests green; `tsc --noEmit` clean.

---

## Phase B — Game Format Engine

*The foundation. Phase C (Leaderboard), D (Blind Draw), and F (History) all depend on this.*

> **Phase B is fully shipped.** Math layer (5 formats — 2-Ball, 3-Ball, Best Ball, Stableford Standard, GOBS Stableford — plus override engine, 164 unit tests, 5 snapshots), database foundation (B4.1–B4.5 ✅), format gate (B1.1–B1.6 ✅), and override / scoring-basis admin UI (B3.1–B3.3 ✅). Phase C status tracked in its own section below.

### B.1 — Format gate & state machine

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| B1.1 | Round state machine | ✅ | States: No Round → Setup → Teams Built → **Format Chosen** → Scorecards Unlocked → Scoring → Complete |
| B1.2 | "Format not set" home banner (admin view) | ✅ | Yellow banner when round needs format. "Choose Format" CTA opens picker. |
| B1.3 | Scorecards locked until format chosen | ✅ | Players can build teams pre-format, but scorecard +/− buttons disabled until format locks. Polite "Waiting for admin to pick today's format" banner. |
| B1.4 | Format picker UI | ✅ | Bottom sheet on mobile, modal on desktop. Five format choices. |
| B1.5 | No defaults, no persistence | ✅ | Every round starts blank. Forces fresh choice. |
| B1.6 | Format locks at first score | ✅ | Once any team enters a hole-1 score, format is read-only. Editing requires dangerous-action modal that warns about score recalculation. |

### B.2 — The five formats

*Format set rebalanced 2026-05-09 + 2026-05-10. GOBS House dropped (A1.4); `stableford_modified` renamed to `gobs_stableford` and now defaults to the locked league table (A1.1). Best Ball added (A1.2).*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| B2.1 | 2-Ball | ✅ | Existing logic. Best 2 net per hole. |
| B2.2 | 3-Ball | ✅ | Best 3 net per hole. 4-player teams drop worst; 3-player teams all count. |
| B2.3 | Stableford Standard | ✅ | Net-based locked table (2026-05-10): Bogey 1, Par 2, Birdie 3, Eagle 5, Albatross 8, Double Bogey or worse 0. Table NOT editable per round — values are constants in `engine.ts`. Team total = sum across all members. |
| B2.4 | GOBS Stableford | ✅ | New format (Phase A.1 / A1.1, renamed from `stableford_modified`). League-specific default table (2026-05-10): Albatross +8, Eagle +5, Birdie +3, Par +2, Bogey 0, Double Bogey or worse −1. Net-based, sum across all members. Negative team totals possible. Point values are **editable per round** via the FormatPicker UI (A1.5) — overrides land in `format_config.point_values` and the engine merges them via `mergePointTable(GOBS_STABLEFORD_POINTS, …)`. |
| B2.5 | Best Ball | ✅ | New format (Phase A.1 / A1.2). Strict best-1 net per hole regardless of team size (2/3/4 players). Net only — net/gross toggle disabled in picker with caption "Best Ball is always net." Best-N family format → override holes apply normally (override = all scores count on that hole). |

### B.3 — Per-hole overrides

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| B3.1 | "All scores count" hole multi-select | ✅ | 18-button grid (6×3) inside the FormatPicker, plus "9 & 18" preset and "Clear all" utility. Stored at `format_config.override_holes`. Engine applies overrides to 2-Ball, 3-Ball, and Best Ball (all best-N family). Stableford Standard / GOBS Stableford are documented no-ops (every player already contributes). Picker section renders muted with an inline "(no effect on Stableford formats)" caption when a Stableford format is selected. |
| B3.2 | Net vs gross toggle | ✅ | Segmented control inside FormatPicker, stored at `format_config.scoring_basis` ("net" \| "gross"). Default "net" via `getScoringBasis()` helper for backward compat with pre-B3.2 rounds. Engine integration uses the zero-handicap trick at the call sites (scorecard + summary): when scoring_basis is "gross", every `courseHandicap` is passed as 0 so the engine's net pathway returns gross-equivalent values uniformly across all formats — Stableford included, since it has no internal `basis` branch. |
| B3.3 | Override visibility on scorecard | ✅ | Soft yellow banner ("All scores count on this hole") rendered between the "Hole N / PAR / YDS" header and the team net pill when the active hole is in `format_config.override_holes`. Visible to all users. Banner is gated on best-N formats since override_holes is an engine no-op for Stableford. |

### B.4 — Database changes

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| B4.1 | Add `format` column to rounds table | ✅ | Enum after 2026-05-10 rebalance: `2_ball`, `3_ball`, `best_ball`, `stableford_standard`, `gobs_stableford`. Original enum (B-phase ship): `2_ball`, `3_ball`, `stableford_standard`, `stableford_modified`, `gobs_house`. Migration `003_phase_a1_format_set_rebalance.sql` handles the swap (drop `gobs_house`, rename `stableford_modified` → `gobs_stableford`, add `best_ball`). |
| B4.2 | Add `format_config` JSON column | ✅ | Stores point values, override holes, net/gross |
| B4.3 | Add `format_locked_at` timestamp | ✅ | Records when first score was entered |
| B4.4 | Backfill existing rounds as `2_ball` | ✅ | One-time migration. Database is being cleared anyway, so trivial. |
| B4.5 | Update scoring engine to switch on format | ✅ | Single function takes (format, scores, handicaps, overrides) → team score. Each format is its own pure function. |

**Phase B exit criteria:** Admin can pick any of the 5 formats (2-Ball, 3-Ball, Best Ball, Stableford Standard, GOBS Stableford), scorecards behave correctly for each, scores calculate correctly, format is locked once scoring starts. Format set rebalanced 2026-05-09 — see Phase A.1.

---

## Phase C — Leaderboard Rework

*Depends on Phase B for format-aware display.*

> PR 1 (C3) ✅ shipped. PR 2 (C1+C2) ✅ shipped. PR 3 (C4+C5+C6) ✅ shipped 2026-05-17 (PM2) — drill-in summary with F9/B9 split + two-level tap-to-expand.

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| C1 | Team-only display during live rounds | ✅ | Existing season-individual leaderboard relocated to `/season` (verbatim, no refactor). New `/leaderboard` is a team-focused live view with four states (no round today / no format yet / live mid-round / completed). Empty state includes a small "View season stats →" link to `/season`. Bottom nav unchanged — still points to `/leaderboard`, now the team view. |
| C2 | Row format: team name + cumulative score + "thru N" | ✅ | Per-row: rank badge (gold for 1st, navy otherwise) → team name + dot-separated roster → score block (24px, color-coded: green under par / red over par / black even / blue Stableford points) → "thru N" (live) or "Final" (complete). Format-aware ranking via pure helper `src/lib/leaderboard/rank.ts` — ascending for best-N, descending for Stableford-family, ties share rank with the next position skipped. "thru N" = count of holes where every required team player has entered a score. Whole row taps through to `/round/[id]/summary` (PR 3 will rebuild that drill-in). **Update 2026-05-20:** per-team "THRU N" / "FINAL" caption now rendered directly on each team card in `RoundResultsView` (beta feedback #3, commit `c89a504`). Previously `TeamRow.thru` was computed in `results.ts` but never displayed. Replaced the bare "Net" / "Net pts" caption below the 24px score with: "FINAL" (complete rounds), "THRU N" (in-progress, N > 0), or "—" (team not yet started). Header-level "In progress · thru {maxThru}" `StatusTag` retained as round-level summary. 5 new tests in `tests/components/round/RoundResultsView.test.tsx`; 344/344 green. |
| C3 | Format-aware score display | ✅ | Format-aware team total display via `formatTeamTotal` helper in `src/lib/format/copy.ts`. Best-N: stroke delta `+N` / `−N` / `E`. Stableford-family: `${total} pts` with Unicode minus on negative totals (originally GOBS House; post-2026-05-09 rebalance, GOBS Stableford carries the negative-going values via Phase A.1 / A1.2). Helper logic continues to handle Stableford-family negatives uniformly after format-set rebalance. Applied at scorecard team pill and round summary (Stableford branch only — best-N summary preserves existing absolute-total display; PR 3 will conform summary to delta convention). |
| C4 | Tap row → round summary view | ✅ (2026-05-17 PM2) | Leaderboard rows already deep-link to `/round/[id]/summary` (shipped with C2). Summary page rebuilt this session as a ranked all-teams view: `rankTeams` from `src/lib/leaderboard/rank.ts`, gold rank-1 badge / navy others, format-aware total color (green / red / black for best-N delta, blue for Stableford pts) via `formatTeamTotal`. Read-only `FormatChip` in header. Status tag: green "Final" if complete, secondary "In progress · thru N" if live (max thru across teams). |
| C5 | Per-player dropdown in summary | ✅ (2026-05-17 PM2) | Two-level drill: team chevron expands to player rows; player chevron expands to `PlayerHoleGrid` (the A1.7 component) with `showRunningTotal={false}` and `currentHoleIndex` omitted. Multi-expand at both levels. Player row right side shows `Gross [N]` + `Net [delta or pts]` side-by-side; Net colored by performance (best-N delta colors, blue for Stableford). **Update 2026-05-17 PM3:** restored an `Individual Rankings` section below all team cards — flat read-only list of every player across every team, ranked by net (best-N ascending, lowest wins) or by points (Stableford descending), tie-skip semantics matching `rankTeams`. Section header at the same visual weight as team-card headings, 1st-place rank styled gold to match the team rank badges. Uses already-loaded data — no new queries. **Update 2026-05-17 PM4:** the round-results surface (date + FormatChip + course + status tag + ranked team cards with drill-down + Individual Rankings) is now a shared `<RoundResultsView/>` component (`src/components/round/RoundResultsView.tsx`) backed by a shared `loadRoundResults(roundId)` helper (`src/lib/round/results.ts`). Both `/round/[id]/summary` and `/leaderboard` render it — tapping a team on `/leaderboard` now expands inline (no extra page navigation) for today's round. Past rounds will still route through `/round/[id]/summary` once the History tab ships. |
| C6 | Front 9 / Back 9 / Total 18 breakdown | ✅ (2026-05-17 PM2) | F9 / B9 leg row rendered below the dot-separated roster on every team card: "F9 [val] · B9 [val]" with leg values computed from engine `perHole` (best-N: per-hole `teamScore − par × contributingPlayerIds.length`; Stableford: absolute team points, since `teamParAtScored == 0` collapses to absolute by C3 convention). Total 18 is the big number on the right side of the card. |

**Phase C exit criteria:** Players check leaderboard mid-round, see clean team rankings, can drill into any team's detail without editing. ✅ Met 2026-05-17 PM2 with PR 3 (C4 + C5 + C6).

> **Phase C follow-up (resolved 2026-05-08, conformed 2026-05-17 PM2):** the scorecard-pill (delta) vs round-summary (raw absolute) stroke convention split has a chosen direction. Resolved by PR 2 leaderboard convention (delta everywhere). Round summary now conforms — PR 3's rebuild uses `formatTeamTotal` for the team total and the same delta convention for player Net.

---

## Phase D — Blind Draw ✅

*Depends on Phase B. Touches scoring engine. Reprioritized ahead of Phase C PR 3 on 2026-05-09 — Dad: blind draw applies roughly every other round due to typical odd-player counts, needs real-round testing before more leaderboard polish. **Shipped 2026-05-18.***

### D.1 — Blind draw ✅ (shipped 2026-05-18)

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| D1.1 | Round-start short team handling — implicit | ✅ | No special flow or marker. A team built with fewer players than the largest team in the round naturally has empty slots. Comparison happens at finalize (max team size in round → any team below = short). No "lock as short team" button; no team-size picker. |
| D1.2 | Mid-round dropout flow | ✅ | Per-player `⋯` overflow menu on BOTH admin RoundSetup active view AND live scorecard. Two-step DangerModal: "Mark [Name] as left round → Which hole did [Name] play through? [1–17 dropdown] → Confirm." Stores `dropped_after_hole` on `round_players`. Row shows muted "(left after hole N)" caption; live scorecard disables +/− for post-drop holes. Audit log row in `round_player_actions` (action, hole, surface=admin\|scorecard, timestamp). Overflow menu consolidates Remove from round + Mark as left + Undo left into ONE icon (refined from spec during handoff — replaces standalone Remove icon to avoid mis-tap for the 60–80yo demographic). |
| D1.3 | Round-start short team flow | ✅ Merged into D1.1 | No standalone flow needed — handled by D1.1 (implicit comparison at finalize) + D1.5 (engine treats round-start short and mid-round dropout slots identically). |
| D1.4 | Mid-round "Pending blind draw" leaderboard label | 💡 DORMANT | Deferred 2026-05-17 during D.1 design. Decoupling start-line from benchmark (benchmark computed at finalize, not at first score) eliminated the race condition where Team 2's card might not exist when Team 1 starts scoring. Live leaderboard now shows real scores for every team; blind-draw reveal happens at finalize alongside score reveal. Revisit if league feedback suggests they want a heads-up that a team is short before round end. |
| D1.5 | Randomizer engine — explicit per-team submission | ✅ | **Initially built as auto-fire on last completed score; rewritten same session to per-team Submit Final Scores after live test surfaced a premature-finalize regression** (A6's "first +/− tap commits par" interacted badly with the completion-check trigger — fixed in `73de23c`). Final design: each scorecard has a "Submit Final Scores" button, disabled until all expected holes scored. Tap → DangerModal "Submit Team [N]'s final scores?" → confirm appends `team_number` to `format_config.submitted_teams`. When the array contains every team in the round AND completion check passes, RPC `finalize_round_with_blind_draws` fires. RPC: locks rounds row FOR UPDATE, runs completion check (per-player expected holes), identifies short teams (max team size benchmark), draws from pool excluding dropouts + already-drawn + own-team roster, in `team_number` ASC order (round-start fills first, then dropout fills by ascending dropout hole), with `setseed()` + `random()` (pool ordered by `round_players.id ASC` for reproducibility — same seed stamped on every `blind_draws` row). Inserts rows + flips `is_complete = true` in single transaction. Returns `'not_yet' \| 'already_complete' \| 'finalized' \| 'pool_too_small'`. Single-fire guard via DB trigger `scores_reject_on_complete` (raises `P0001 round_finalized`) — replaces spec's "API layer rejection" since no API layer exists. WriteQueue classifies P0001 as terminal with specific stale-failure copy ("Round was finalized — scores can no longer be edited"). |
| D1.6 | Multiple short teams | ✅ Merged into D1.5 | No special branch — D1.5's draw-order + no-collisions pool rule handles N short teams cleanly. |
| D1.7 | Pre-fire warning banner | ✅ | NEW. Yellow banner above the hole-scoring UI on the team-filtered scorecard view (`?team=N`). Renders when (a) this team is the only one not in `format_config.submitted_teams` AND (b) this team has all 18 holes scored. Copy: "All other teams have submitted. Tap Submit Final Scores when ready." Not dismissible; disappears when round finalizes or another team becomes incomplete. Whole-round view opts out (no team to wait on). |
| D1.8 | Post-fire confirmation toast | ✅ | NEW. After RPC returns `'finalized'`, brief toast (≈4s): "✅ Round finalized. Blind draw complete." Visible to whoever's submit closed the set. |
| D1.9 | Post-round result display in RoundResultsView | ✅ | NEW. Adds blind-draw rendering to the shared `<RoundResultsView/>` (built by C PR3 extraction). Leaderboard team row: muted "🎲 Blind draw: [Name] ([range]) — Net/Gross/Pts X" pill below the roster (drawn player's aggregate over the fill range, format-aware via `scoring_basis`). Expanded team card: pairs dropout fills with their dropped player by `dropped_after_hole + 1 = hole_range_start`; paired players show "left after hole N" inline note + an expanded merged 18-hole grid + caption "🎲 Holes N+1–18: blind draw from [Name] (Team M) — [Net/Gross/Pts] X". Round-start fills render as synthetic pseudo-player rows with their own expand state. Read-only post-finalize scorecard: +/− buttons removed; the big-number per-hole display shows the drawn player's score with a "🎲 (blind draw)" caption when the hole falls in a paired fill's range; expanded `PlayerHoleGrid` shows merged scores. Individual Rankings section EXCLUDES blind-draw fills AND dropouts (only complete-round players ranked). New `BlindDrawFill.drawnPlayerNetValue` field on data layer; `results.ts` refactored to two passes (precompute `enginePerTeam` map, then assemble TeamRows with cross-team engine lookups for the aggregate). |
| D1.10 | Score-write client guard for submitted teams | ✅ | NEW (added during live testing — commit `c309e18`). Closes integrity gap between team submit and round-level finalize: DB trigger only rejects post-`is_complete` writes, but `format_config.submitted_teams` flips earlier. Client-side guard added in score-write path: if player's `team_number` is in `submitted_teams`, abort write with 3.5s orange toast "This team's scores are locked." `RoundPlayer.team_number` now loaded so the per-player check works in the whole-round view too. Same commit fixed a missed `isLocked` gate on the `+` button (the earlier `replace_all` had different surrounding context and only caught `−`). **Superseded 2026-05-20 by D1.11:** the client-side score-write guard was removed when the DB trigger was dropped. The per-team UI lock (`isLocked` from `myTeamSubmitted`) still hides +/− buttons on submitted teams — the deleted guard was a defense-in-depth abort after the UI already hid the buttons. |
| D1.11 | Drop DB-level finalize lock; move to UI lock + admin edit mode | ✅ (2026-05-20) | Migration `009_phase_d1_drop_scores_finalize_trigger.sql` drops trigger `scores_reject_on_complete` and function `reject_scores_on_complete_round`. Client-side `submitted_teams` score-write guard removed (along with the dead `lockedToastVisible` state + toast JSX). Finalized rounds remain read-only by default in the UI; admin opens edit mode via `?admin=1` URL flag → "Edit Round Scores" button on the round summary → DangerModal confirm → navigates to `/round/[id]/scorecard?admin=1&edit=1`. Edit mode is round-scoped (URL params) and persists via the new `src/app/round/[id]/layout.tsx` that pins `<EditModeBanner/>` to the top of every round-scoped page. Done button drops `?edit=1`. Blind-draw fill rows stay read-only — their score is computed from the source player's actual scores, so editing the source flows through automatically. Totals recalculate via the existing pure engine. WriteQueue P0001 classifier left in place as defensive scaffolding. Includes the H1 route rename from `/thomas-admin` to `/admin` (4 import paths + nav link + docs). 26 new tests; 298/298 passing. |
| D1.12 | Blind-draw fill rows render real per-hole pars (not hardcoded par-4) | ✅ (2026-05-20) | `BlindDrawFill.drawnPlayerPar` was always set as 18-element `Array(18).fill(4)` in `results.ts`. Fix: look up the drawn player's tee from `holesByTee[lookup.teeId]` and build the real par array, same as `PlayerRow.par`. Threaded through `BlindDrawPseudoPlayerSection` → `PlayerHoleGrid`. Regression test added to `PlayerHoleGrid.test.tsx` (all-par-3 F9 subtotal = 27, not 36). `d2556a6`. |

**Phase D.1 exit criteria:** Short teams (round-start and mid-round dropout) handled gracefully on both ends. Each team explicitly submits final scores via per-team button; randomizer auto-fires only when every team in the round has submitted. No collisions across short teams within a round. Pre-fire warning banner appears on final group's scorecard when they're the last unsubmitted team with all holes entered. Post-fire toast confirms completion. Blind-draw fills visible on leaderboard, summary, and read-only scorecard with 🎲 visual treatment — including the drawn player's aggregate score (Net/Gross/Pts per format) inline. Individual Rankings excludes fills and dropouts. Post-submit scorecard fully read-only (no +/−, no submit button, no overflow menu); client-side score-write guard prevents stale-tab edits.

(Phase D.2 / Rainout deleted 2026-05-09 — league rule: if play stops, no payouts, round doesn't count. No app-side partial-round handling needed. See Decisions Locked.)

---

## Phase E — Played-With Redesign

*Independent of B/C/D. Can run in parallel.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| E1 | Egocentric view (mobile + desktop primary) | 📋 | Pick a player, see four buckets: 6+ rounds (bars), 3–5 rounds (tags), 1–2 rounds (tags), Never played (red tags) |
| E2 | Improved sortable grid (desktop secondary) | 📋 | Linked from egocentric view as "Show full grid." 50×50 with color intensity, sort options (alphabetical, handicap, total rounds), gaps visually distinct |
| E3 | Tap any player/cell → detail | 📋 | Shows exact count and last round together |
| E4 | Last-played-together computed field | 📋 | New lookup against existing scores tables |
| E5 | Season scope filter | 📋 | All-time vs. this-season toggle. Affects query. |

**Phase E exit criteria:** Admin can answer "who has Bill played with?" in two taps on mobile, and "show me league-wide patterns" on desktop.

---

## Phase F — History & Betting Tab Split

*Mostly independent. Format-aware display in Phase B helps but isn't strictly required.*

### F.1 — History tab

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| F1.1 | Round summary line | 📋 | Date, format used, # teams, players per team, total players, winning team |
| F1.2 | Expanded view: full team breakdowns | 📋 | All teams, players, final scores, glanceable without click |
| F1.3 | Click round → full scorecards page | 📋 | Drill into past round, see every team's hole-by-hole, read-only |
| F1.4 | Click player row in scorecard → individual round detail | 📋 | Hole-by-hole for that player on that day |
| F1.5 | Date navigation | 📋 | Vertical list gets long across seasons. Need a date picker, season filter, or grouped-by-month view. |

### F.2 — Betting tab

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| F2.1 | Per-player season summary view | 📋 | Each row: player name, rounds played, cumulative won/lost, average per round |
| F2.2 | Click player → full per-round history | 📋 | Win/loss for each round they played that season |
| F2.3 | Persistent season totals header strip | 📋 | Total buy-in, HiO Fund balance, BFB Fund balance (donated YTD), total paid out |
| F2.4 | Per-round money breakdown | 📋 | Buy-in × players, HiO contribution, BFB contribution, pot, payouts |
| F2.5 | Buy-in snapshot per round | 📋 | Default $10, but stored on round (not global setting) so historical rounds preserve their amount even if defaults change later |
| F2.6 | Manual edits with dangerous-action modal | 📋 | Admin-only. Money disputes need fast resolution path. |
| F2.7 | Admin-only access | 📋 | Players don't see betting tab. Don't want it getting competitive. |
| F2.8 | BFB Fund visibility on home page | 📋 | Surface running BFB total on the home page so league sees the charity element. Annual donation drive is in July. Possible future addition: drive-specific tracking, contribution nudges. Added 2026-05-09. |

**Phase F exit criteria:** Dad can answer "how much has Bill won this season?" and "what's the BFB fund at?" without opening a spreadsheet.

---

## Phase G — Money / Payout Engine

*❓ Blocked on Dad's answers. Scaffold the data model and UI now; fill in the math when his answers come back.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| G1 | Buy-in records per round per player | 📋 | Default $10 each, configurable per round, mark "didn't buy in" for guests/late arrivals |
| G2 | Payout records per round | ❓ | Need: who wins, how much, how the pot splits |
| G3 | Fund balance computed views | 📋 | Running totals: HiO fund, BFB fund. Computed from contributions − payouts. |
| G4 | HiO Fund payout flow | 📋 | Manual button: "Pay out HiO Fund" creates payout record, resets fund. Logged for whenever a hole-in-one happens. |
| G5 | BFB Fund yearly donation flow | 📋 | At end of season, admin records donation. Resets fund. |
| G6 | Auto-calculate winnings from scores | ❓ | The end goal — scores in, money out. Blocked on team pot rules. |

**Phase G exit criteria:** Dad never has to manually calculate winnings. Money math is automatic and accurate to the dollar for end-of-season BFB donation.

---

## Phase H — Pre-Launch Hardening

*Stuff that's easy to forget but matters before going live full-time.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| H1 | Hide admin button from homepage | ✅ (2026-05-20) | Route renamed `/thomas-admin` → `/admin` as part of the D1.11 ship. Homepage "Admin" link target updated; folder + 4 internal imports updated. URL still accessible to those who know it. |
| H2 | Database backup strategy | 📋 | Daily Supabase backups. Manual export option for end-of-season snapshot. **Treated as blocker for historical data import and full production use.** Severity bumped 2026-05-09 — Dad asked May 9 if he could enter all 2026 historical rounds; answer is no until backup + partial-reset workflow is in place. Should ship after Phase A.1 / Phase D.1, before Phase E. |
| H2.5.1 | Add `handicap_index_snapshot numeric` column to round_players | 📋 | Migration adds nullable column. Backfill: UPDATE round_players SET handicap_index_snapshot = (SELECT handicap_index FROM players WHERE players.id = round_players.player_id) for all existing rows. Acceptable to backfill from current HI because no HI has changed since the small number of existing rounds were played. |
| H2.5.2 | Update all round_players INSERT paths to set snapshot | 📋 | Touches: src/app/admin/tabs/RoundSetup.tsx (toggleInRoster, goToTeams), src/app/page.tsx (Commit 4 team formation handlers — create_new, confirm_join, Manage Team add flow), src/app/round/new/page.tsx, src/app/round/[id]/scorecard/page.tsx (any path that inserts a round_players row). Snapshot value = current players.handicap_index at insert time. |
| H2.5.3 | Update CH math to read from snapshot | 📋 | All callers of computeCourseHandicap currently read from players.handicap_index (directly or via join). Switch to round_players.handicap_index_snapshot. Touches scorecard, summary, leaderboard, RoundResultsView. |
| H2.5.4 | Gate LT1 self-heal on round.is_complete = false | 📋 | Self-heal in src/app/round/[id]/scorecard/page.tsx currently recomputes CH on every scorecard load. Add guard: skip self-heal when round.is_complete is true. Self-heal still reads from the snapshot, not players.handicap_index. Closes the "Friday's HI update silently shifts Thursday's round" bug. |
| H2.5.5 | Admin HI edit cascade to active rounds | 📋 | In src/app/admin/tabs/Players.tsx, when admin updates players.handicap_index, also UPDATE round_players.handicap_index_snapshot for every row where round_players.player_id = edited player AND rounds.is_complete = false. Single transactional update preferred. Finalized rounds untouched. |
| H2.5.6 | Test surface | 📋 | Unit tests covering: (a) snapshot is set on insert across all entry points, (b) self-heal does not fire on finalized rounds, (c) admin HI edit cascades to active rounds only, (d) CH math reads from snapshot. Integration test: change a player's HI, verify a finalized round's stored CH does not change. |
| H3 | Season open/close flow | 📋 | Admin manually closes season in Settings. Reminder banner appears as season-end approaches (Sept/Oct). Closes the BFB fund for donation. |
| H5 | Data import for 4 weeks of historical rounds | 📋 | Once Dad fills in the spreadsheet, import script reads it and writes to Supabase. One-time job. Gated on H2 partial-reset workflow. Depends on H.2.5 — importer writes per-round HI values into round_players.handicap_index_snapshot, not players.handicap_index. |
| H6 | QR code for current URL | 📋 | One-time deliverable. Regenerate when custom domain ships. Added 2026-05-09. |
| H7 | "Add to Home Screen" instructions doc | 📋 | Short numbered guide (iOS Safari Share → Add to Home Screen, Android Chrome equivalent). For Dad to forward to league. Doc deliverable, not code. Added 2026-05-09. |
| H8 | One-shot DB export for Dad's manual verification | 📋 | Export per-player handicap config, per-tee data, hole yardages as screenshots/CSV. Dad will manually verify end-to-end. Added 2026-05-09. |

**Phase H.2.5 exit criteria:** Changing a player's handicap_index in the admin Players tab never retroactively alters a finalized round's stored CH, net scores, or leaderboard rank. Historical import (H.5) can supply per-round HI values written directly to the snapshot column.

(H4 — Partial round long-term decision — deleted 2026-05-09. Rainout-cancellation rule moots the decision. See Decisions Locked.)

**Phase H exit criteria:** Dad can switch to the app exclusively without fear of data loss. League can play indefinitely without intervention.

---

## Tech Debt & Follow-Ups

*Surfaced from session work but not tracked in the phase tables. Not phase-blocking.*

| # | Item | Status | Severity | Notes |
| --- | --- | --- | --- | --- |
| TD1 | A4-extended: leftover CH/HC/HCP labels | 📋 | Low | Strokes terminology not yet applied on leaderboard, round/new, summary pages, and admin RoundSetup tab. Phase A only covered scorecard, player tab, profile, admin players tab. |
| TD2 | Supabase array-vs-object pattern audit | ✅ (2026-05-10) | — | Resolved as part of the admin RoundSetup read-path fix (same session). `loadRoundForDate` in `src/app/admin/tabs/RoundSetup.tsx` was the last load-bearing instance — it had been resolving player rows by `allPlayers.find(p => p.id === rp.player_id)` against an active-only prop, which silently dropped any player who'd been deactivated while still rostered. Switched to the embedded join `players ( id, full_name, display_name, handicap_index, is_active )` with the standard `Array.isArray(rp.players) ? rp.players[0] : rp.players` guard. History.tsx / scorecard/page.tsx / summary/page.tsx were spot-checked and already use the embedded-join pattern correctly. |
| TD3 | RoundSetup useEffect dep on `allPlayers` | 📋 | Low | Parent re-creating the array re-runs `loadRoundForDate` unnecessarily. Minor perf, no correctness issue. |
| TD4 | `goToTeams` non-transactional delete-insert | ✅ (2026-05-10) | — | Replaced delete-all + reinsert with a diff-based reconciliation in `goToTeams`. Computes `toRemove = existing.player_ids \ roster.player_ids` and `toAdd = roster.player_ids \ existing.player_ids`, then only deletes the former and inserts the latter. Players present in both sets are never touched, so a partial failure can no longer wipe out team assignments. No DB transaction / RPC needed — the failure mode is eliminated structurally rather than wrapped. |
| TD6 | Hardcoded `format: "2_ball"` in scorecard / summary engine calls | ✅ (resolved 2026-05-07) | Medium | `src/app/round/[id]/scorecard/page.tsx` (lines ~227, 287) and `src/app/round/[id]/summary/page.tsx` (lines ~144, 150) pass `format: "2_ball"` and a synthetic `formatConfig` to the scoring engine. Should read from `rounds.format` / `rounds.format_config`. Currently masked because the only shipped format is 2-Ball-equivalent at the engine level; will visibly diverge once admins start picking other formats. |
| TD7 | Editable point values UI for GOBS Stableford | ✅ (2026-05-10) | — | Originally scoped against Stableford Modified; recast 2026-05-10 when Modified was renamed to `gobs_stableford` (A1.1). Shipped under Phase A.1 / A1.5 — six editable rows in FormatPicker (Albatross / Eagle / Birdie / Par / Bogey / Double Bogey or worse), clamp [−10, +10], "Reset to defaults" link, persists to `format_config.point_values`, mid-round edits warn via the existing DangerModal flow. |
| TD8 | Banner button clipping on narrow phones (<414px) | 📋 | Medium | The "Choose Format" CTA inside `FormatNotSetBanner` is clipped at 375px (iPhone SE) and similar small-phone widths. Surfaced during B1.5 screenshot capture. League demographic includes older players on smaller/older phones, and the banner is the admin's primary entry point. Likely fix: stack banner contents vertically below ~414px, or shrink CTA padding. Visual-only change, no logic. |
| TD9 | Player-visible format display on scorecard | ✅ (resolved 2026-05-07) | Low-Medium | Read-only `FormatChip` rendered in the scorecard header (above "Hole N" / par-yardage caption) once a format is locked. Visible to all users, not admin-only. No `onChange` prop, so the chip is non-interactive on the player surface — admins still change format from the chip on `/admin`. |
| TD10 | Remove stale "2-ball" toggle in admin > Settings | ✅ (2026-05-10) | Low-Medium | Toggle + Scoring Card removed from `Settings.tsx`. `two_ball_scoring` removed from `ToggleKey` and from the seed defaults in `admin/page.tsx` (existing rows in `league_settings` are left untouched — UI just stops surfacing the key). No other v1 leftovers spotted in the Settings tab — "Coming Soon" placeholders for handicap rules / season range / payout structure remain as roadmap stubs. |
| TD11 | Snapshot scripts need format filter guard in live-data regression loop | ✅ (2026-05-10) | Medium | Added `if (round.format && round.format !== "2_ball") continue;` to the Part 1 loop in `snapshot-b2`, `snapshot-b3`, `snapshot-b4`, `snapshot-b5` and the new `snapshot-b6`. Synthetic Part 2/3/… cases unchanged. Any future non-2-Ball production round now skips the legacy 2-Ball comparator instead of producing false-positive mismatches. |
| TD12 | FormatPicker now requires explicit Save even for fresh rounds | 📋 | Low | Pre-B3.x flow was one-tap-and-save: admin tapped a format and the picker closed, committing immediately. Post-B3.x flow is two-step: admin taps a format → basis + override-holes sections render → admin taps Save. Justified by the new rules sections needing a confirmation step, but it's measurable friction for the common "just pick 2-Ball, defaults are fine" path. Watch for admin pushback in real-world use. Likely fix if it bothers users: add a "Quick Save" / "Save with defaults" affordance for fresh rounds (no `format_locked_at`, no existing config) so a single tap commits format + default config; the rules sections still appear for anyone who wants them. Don't pre-build — wait for actual feedback. |
| TD13 | `show_leaderboard` admin toggle now gates the wrong route | ✅ (2026-05-10) | Low | Settings UI label / description updated: "Show Season Stats" / "Visible on the season stats page (/season). Live leaderboard always visible." Underlying DB key left as `show_leaderboard` (per-user instruction to avoid the rename migration — flagged as a future cleanup if the column name ever becomes confusing). Gate still applies to `/season` only; `/leaderboard` was already publicly accessible. |
| TD14 | gitignore + tidy `.claude/settings.local.json` | 📋 | Low | File is currently tracked in git, but per Claude Code convention it should be a per-machine local override. Some entries also embed ad-hoc `curl` commands carrying the Supabase anon JWT — anon keys are public by design (they ship in `NEXT_PUBLIC_SUPABASE_ANON_KEY`), so this is repo hygiene, not a credentials leak. Fix when tackled: add to `.gitignore`, `git rm --cached`, and scrub the curl entries down to permission-pattern names if anyone still wants the precedents. |
| TD15 | Deactivate-while-rostered has no guard | 📋 | Medium | `Players.tsx` deactivate flow (`doDeactivate`) is a single tap + confirm modal that flips `is_active = false` with no check whether the player is currently in `round_players` for an in-progress round. Risk: Dad deactivates a player mid-round, the player keeps rendering in the admin RoundSetup view (per the 2026-05-10 embedded-join fix that intentionally shows inactive players who are still rostered), but they're no longer in the check-in list — so re-adding them after an accidental deactivation requires reactivating first. Likely fix: query `round_players` for the player_id with `is_complete = false` join before allowing deactivation; if any match, block with a clearer modal ("This player is in today's round. Remove them from the round first, then deactivate.") or warn-and-confirm with the round date shown. Surface upstream from `loadRoundForDate`'s defensive render. Not load-bearing today because the read path is robust to inactive rostered players, but the UX is brittle and could cause data confusion. |
| TD16 | Sentry source-map de-minification verification | 📋 | Low | Sentry phase 1 plumbing shipped 2026-05-13 with source-map upload wired via `SENTRY_AUTH_TOKEN` in Vercel. Phase E's `writeQueue.terminal_failure` events (with `reason` discriminator) are the first events to actually land in Sentry from custom code. Should verify, the first time one fires in production, that the stack trace is de-minified and readable. Wizard setup is supposed to handle this end-to-end; nothing to fix unless verification turns up minified frames. Track here as a "verify when the first event lands" item rather than building a manual exercise. |
| TD17 | Delete-scorecard affordance on admin RoundSetup | 📋 | Medium | No per-team delete in admin RoundSetup. Surfaced 2026-05-20 when Jonathan had to use the Supabase SQL Editor to delete two test scorecards (Teams 7 + 8) off prod round 103. Likely fix: per-team ⋯ menu in the active-view team card with a "Delete scorecard" option → DangerModal warning "This removes all scores for Team N. Cannot be undone." Don't renumber remaining teams (gaps are fine — admin builds fresh sets). |
| TD18 | Extract `Player` type to `src/lib/types.ts` | 📋 | Medium | `Player` type currently defined in `src/app/admin/page.tsx`. Five team-formation files (`PlayerPickerSheet.tsx`, `ManageTeamSheet.tsx`, `smartJoin.ts`, `page.tsx`, scorecard `page.tsx`) now import from there — fragile if `/admin/page.tsx` is ever refactored or moved. Extract to `src/lib/types.ts` alongside `RoundPlayer`. |
| TD19 | Migrate `/round/new` to use lifted `ensureRoundShell` | 📋 | Low | The `/round/new/page.tsx` inline round-creation pattern was intentionally left when `ensureRoundShell` was lifted to `src/lib/round/` (Commit 1, 2026-05-20). The inline pattern is tangled with team-number resolution logic that doesn't exist in the shared helper. Deferred; clean up when touching that file for another reason. |
| TD20 | `withAdminFlags` exported from `src/lib/admin.ts` but unused in production | 📋 | Low | Helper propagates `?admin=1&edit=1` onto relative hrefs. Will be used when summary↔scorecard linking surfaces (currently, admin navigates to the scorecard by confirming the Edit Round Scores DangerModal on the summary — no inline link exists). Leave until that linking is built; don't delete prematurely. |
| TD21 | LT1 self-heal documentation post-H.2.5 | 📋 | Low | After H.2.5 ships, the LT1 self-heal entry in Decisions Locked > Terminology > Course Handicap (CH) needs a one-line amendment: "Self-heal reads from round_players.handicap_index_snapshot and only fires on rounds where is_complete = false." Cosmetic doc update, no code. |

---

## Phase I — Post-Launch / Nice-to-Haves

---

*Parking lot. None of these block launch. Revisit after a few months of real-world use.*

| # | Item | Status | Notes |
| --- | --- | --- | --- |
| I1 | Player profile — round history accordion | ✅ (2026-05-10) | Collapsed by default. Tap to expand. Replaces auto-displayed history. Title shows rounds-played count when collapsed. |
| I2 | Player profile — Played With accordion | 📋 | Same pattern. Reuses Egocentric component from Phase E. |
| I3 | Player profile — season stats summary | ✅ (2026-05-10) | Rounds played, avg gross, avg net, best/worst, last-5 vs all-time comparison with trend label, inline SVG sparkline of all rounds, recent-5 scores list. |
| I4 | Player profile — performance chart | 💡 | Line chart of scores over time |
| I5 | Player profile — winnings/losses | 💡 | Deferred. Don't want it competitive. May add later if Dad asks. |
| I6 | Team recommendation engine | 💡 | Suggest balanced teams based on handicap spread + played-with history. **This is where pair-balance math lives, not in played-with view.** |
| I7 | Custom domain | 💡 | gobsgolf.com or similar |
| I8 | PWA install polish | 💡 | "Add to home screen" prompts |
| I9 | SWAT format | ❓ | Blocked. Need rules from Dad. |
| I10 | Combo tees | ❓ | Blocked. Need rules from Dad. |
| I11 | Generate Supabase TypeScript types | 💡 | Run `supabase gen types` CLI so query responses get column-shape type checking. Currently uses `any`, meaning tsc cannot catch column typos. Tooling-only change, no functional impact. Worth doing once schema stabilizes. |
| I12 | Players list — row-level accordion | 📋 | Tap row in `/players` to expand inline panel showing rounds, avg score, best, last round, and link to full profile. Stats lazy-loaded per player; uses complete rounds only. Distinct from I1-I3, which add accordions inside the profile page itself. Data layer (`fetchPlayerStats`) accepts an optional date-range filter so a season scope can be added later without rework. |
| I13 | Admin UI: edit `players.preferred_tee_id` from Players tab | 📋 **Priority — next session after Phase 0.5** | Per-player tee preference column shipped 2026-05-10 (migration `004_phase_a_preferred_tee.sql`, default White/Yellow Combo / id=4, Wayne Vincent seeded to White / id=2). Admin tab needs a tee dropdown on each player row to add/edit/clear `preferred_tee_id`. Until shipped, new exceptions require a direct SQL update. **Priority bumped 2026-05-10 (night):** roster has two Waynes (`id=45 Wayne Hashimoto / Wayne H` and `id=55 Wayne Vincent / Wayne V`). Only Vincent has `preferred_tee_id` set. Setting Hashimoto's later (or any other future exception) via direct SQL carries real risk of editing the wrong row. Ship I13 right after Phase 0.5 lands so admin can set preferences safely. |
| I14 | Mid-round tee change UI | 💡 | Per-scorecard one-off tee override (e.g., "Bob is playing Blue today"). Currently the Tee Selection screen on the scorecard is the only path; once a player's tee is committed (every new round, via the START ROUND bulk-commit) there's no in-app way to change it. Surface as a dangerous-action modal on a player row in the live scorecard. Deferred — rare in practice, wait for actual demand before building. |
| I15 | Per-device player identity (lightweight) | 💡 | First-launch "Who's using this phone?" picker. Selection stored in localStorage, used to: hide Start a Scorecard when player already on a team, pre-check that player in Manage Team picker, gate Manage Team to current team members, personalize homepage. No password, no real auth — just a device-identity hint. Switchable via a "Switch user" link. Added 2026-05-20 in anticipation of a simplified team-formation flow that intentionally lands without device identity; this entry captures the deferred improvement. Defer until post-launch friction proves it's worth. |

---

## Open Questions for Dad

*All collected in one place. As Dad reviews the rules document, his answers unblock items here.*

| # | Question | Blocks | Source |
| --- | --- | --- | --- |
| Q1 | "Use handicaps for first 8 holes" — what does this mean exactly? | Affects every round's net calc | Rules doc Section 2 |
| Q2 | Who wins the team pot? 1st place only or top 2? | G2, G6 | Rules doc Section 8 |
| Q3 | Gross or net for team pot winner? | G2, G6 | Rules doc Section 8 |
| Q4 | How does winning team split the pot? | G2 | Rules doc Section 8 |
| Q5 | Does the pot carry over on ties? | G2 | Rules doc Section 8 |
| Q6 | What happens to buy-in if player leaves mid-round? | G1 | Rules doc Section 8 |
| Q7 | Do guests buy in same as members? | G1 | Rules doc Section 8 |
| Q9 | Handicap data for 4 missing players (DeWaal S, Gary T, Gerry H, Norm C) | Phase A onwards | Players Reference sheet |
| Q10 | What does SWAT stand for? Rules? | I9 | Rules doc Section 10 |
| Q11 | What is a SWAT Scramble? | I9 | Rules doc Section 10 |
| Q12 | How do combo tees work? | I10 | Original feedback |

*(Q8 deleted 2026-05-09 — rainout cancellation rule moots partial-round handling. See Decisions Locked.)*

---

## Open Design / Technical Decisions

*Things we've parked but not lost.*

| # | Decision | Status |
| --- | --- | --- |
| D1 | Admin access protection — URL-only vs. real login | URL-only for now. PIN gate logged for future. |
| D2 | Default-to-par scorecard alternative | Logged. May revisit if dash interaction confuses players. |
| D3 | Pre-format-lock scoring (Option B) | Rejected. Logged in case Option A creates friction. |
| D4 | Multiple short teams in one round | Independent draws. Watch for edge cases. |
| D5 | Compare-two-players feature | Not in current scope. Logged. |
| D6 | Played-with season scope toggle | In Phase E. Default to "this season." |

---

## Decisions Locked

*Everything below is settled. Don't relitigate without explicit conversation.*

### Terminology

*Locked 2026-05-08, ahead of first live golf-course test. Supersedes the prior "Strokes terminology replaces CH/HC/HCP" decision from Phase A — that rename was a half-step; the bare "Strokes" label was ambiguous (it was being used for course_handicap on the scorecard but for handicap_index in the admin Players tab). Two distinct quantities, one label. Now disambiguated.*

- **Handicap Index (HI):** Player's portable rating, stored in the `handicap_index` column on `players`. Same value across all tees. WHS calculation; player attribute not round attribute.
- **Course Handicap (CH):** Slope-adjusted strokes received on a specific tee, stored in the `course_handicap` column of `round_players`. Round attribute, computed via `computeCourseHandicap` from the player's `handicap_index` plus the round's selected tee slope/rating/par. Recomputed on scorecard load (LT1 fix, 2026-05-09) so admin edits to HI flow through to the displayed CH without manual intervention; downstream consumers (summary, leaderboard, season) read the value the scorecard wrote back. (The earlier May 8 entry referenced a `player_course_handicaps` table — that table does not exist; `round_players.course_handicap` is the actual storage.)
- **Display rule:** UI never uses bare "Strokes" or "Handicap" alone. Always qualified as "Handicap Index" (or short "HI" where space is tight) or "Course Handicap" (or short "CH"). Database column names (`handicap_index`, `course_handicap`) are already correct and stay as-is.

### Other locked decisions

- **Format gate:** Admin must pick fresh every round. No defaults, no persistence between rounds. Forced choice prevents wrong-format scoring.
- **Format authority:** Admin only. Players cannot pick or change.
- **Scorecard pre-format:** Locked until format chosen. Display "Waiting for format" message.
- **Stat display drop:** Gross removed from scorecard top pill. Kept everywhere else.
- **Stroke-allocation dots:** On the per-hole entry surface, handicap strokes received on the current hole are shown as small navy dots above the +/− buttons. No numeric overlay alongside.
- **Default scorecard value:** Dash with par-anchored +/−. Database null until first tap.
- **Blind draw timing:** Resolves after round finalized, not at start.
- **Blind draw eligibility:** Random pick from any other team's player.
- **Blind draw drawn-player effect:** Original team unaffected; scores copied to short team only.
- **Rainout / partial rounds (locked 2026-05-09):** League rule — if play stops, no payouts, round doesn't count. No app-side partial-round handling. Supersedes the prior "9-hole minimum / sub-9 saved as partial" decision.
- **One format per day, league-wide:** Different foursomes don't play different formats simultaneously.
- **Money allocation:** $1 HiO + $2 BFB + $7 team pot (default $10 buy-in).
- **BFB:** Blaine Food Bank, donated yearly at season end.
- **History/Betting tab split:** Two tabs. History = game performance. Betting = financial.
- **Played-with primary view:** Egocentric (one player at a time) on mobile + desktop. Improved grid as desktop secondary.
- **Pair-level handicap balance:** Not a thing. Balance is team-level. Belongs in team recommendation engine (I6), not played-with.
- **Player money on profile:** Deferred. Don't want it competitive.
- **Admin button on homepage:** Hide for launch. URL `/admin` still works.

### Player-driven team formation (locked 2026-05-20)

Decisions from Phase A.2. Don't relitigate without explicit conversation.

- **Picker scope:** The `PlayerPickerSheet` shows the full unassigned-player pool, not just played-with matches. Played-with history informs the smart-join ranking but all players are selectable.
- **User must tap themselves AND teammates:** A player joining a team taps their own name first (self-selection), then optionally taps teammates to add them. There is no "add player X to team Y" flow for players — only admins drag-and-drop.
- **Smart-join branches:** Three paths: (a) player taps self, no team → `smartJoin` recommends a team or "form new" based on played-with matrix + HI spread; (b) player taps teammate card while in a team → adds that player to the tapper's team; (c) admin can override freely via existing Edit Teams drag-and-drop.
- **Removed player → `team_number = 0`:** Removing a player from a team sets `round_players.team_number = 0` (unassigned), not deletion. The `round_players` row stays; the player re-appears in the unassigned pool.
- **Manage Team hides on first score:** The "Manage Team" button on the scorecard is hidden once any score is entered for the team (`holesPlayed > 0` on any team member). Prevents mid-round roster changes that would break scoring history.
- **Race-condition write queue:** All `round_players` writes from the picker and Manage Team sheet use the per-player write queue pattern (see Locked patterns in CLAUDE.md). Rapid tap patterns (pick player → assign to team) serialize per-player to prevent silent UPDATE-matches-0-rows failures.
- **Bottom-sheet / modal pattern:** `PlayerPickerSheet` and `ManageTeamSheet` are bottom sheets on mobile (<768px) and centered modals on desktop. Consistent with the `FormatPicker` pattern established in Phase B.
- **Device identity deferred (I15):** The picker intentionally ships without per-device player identity. Players select themselves manually each time. Device-local `localStorage` identity (I15) is a deferred improvement; don't build it until post-launch friction proves it's worth the complexity.

### Format set (locked 2026-05-10)

Five formats: **2-Ball, 3-Ball, Best Ball, Stableford Standard, GOBS Stableford**. GOBS House dropped from codebase, format enum, UI, tests, snapshots, and roadmap. `stableford_modified` renamed to `gobs_stableford` (same engine machinery, new defaults + UI). Migration `003_phase_a1_format_set_rebalance.sql`.

### Stableford point values (locked 2026-05-10)

Two distinct Stableford formats with separate point tables. *Standard's table is locked at the engine constant and intentionally has no admin UI. GOBS Stableford's defaults are editable per round via FormatPicker (A1.5) — overrides land in `format_config.point_values`.*

**Stableford Standard** (locked, NOT editable):
- Albatross: +8
- Eagle: +5
- Birdie: +3
- Par: +2
- Bogey: +1
- Double Bogey or worse: 0

**GOBS Stableford** (defaults, editable per-round):
- Albatross: +8
- Eagle: +5
- Birdie: +3
- Par: +2
- Bogey: 0
- Double Bogey or worse: −1

Implications: GOBS Stableford can produce negative team totals when many double-bogey-or-worse holes accumulate; leaderboard rank + `formatTeamTotal` handle negative `pts` via Unicode minus. `getStablefordPoints` collapses any net result of +2 or worse to the doubleBogeyOrWorse bucket (engine confirmed via `delta >= 2` final return).

### Best Ball format (locked 2026-05-09, override behavior clarified 2026-05-10)

- **Selection rule:** strict best-1 — exactly one player's score counts per hole, regardless of team size.
- **Scoring basis:** net only. Format's purpose is handicap equalization; gross best ball undermines the equalizer in a mixed-handicap league. Net/gross toggle disabled in the picker for this format and labeled "Best Ball is always net." FormatPicker also force-flips local state back to "net" any time Best Ball is selected so a stale "gross" choice can't slip through.
- **Override-holes:** apply normally. Best Ball is a best-N (N=1) family format, so an override turns the hole into "all scores count" exactly as it does for 2-Ball / 3-Ball.
- **Engine:** `computeBestNHole` with N=1 via `defaultBestN("best_ball") === 1`.

### Format selection entry point (locked 2026-05-10, supersedes 2026-05-09)

Admin opens the Round Setup tab and sees two top-level buttons: "Today's Format" and "Edit Teams". Either button can be tapped first; both auto-create a round shell for today if none exists. Format and team-building are fully independent — admin can set format with no teams built, or build teams with no format set. Format applies live to every scorecard for the day, regardless of creation order. Players starting scorecards from the homepage trigger round-shell auto-creation too; their scorecard locks with ScorecardLockNotice until admin sets format. No format UI of any kind appears on player surfaces.

### Non-paying player handling (locked 2026-05-09)

Players who opt out of betting are not put on a scorecard. Their team plays as a blind-draw team. No app-side toggle needed for "exclude from betting but keep scoring."

### Handicap Index snapshot (locked 2026-05-20)

- **Snapshot at insert.** Every round_players row stores the handicap_index that was in effect for the player on that round, in a dedicated round_players.handicap_index_snapshot column. All course-handicap math reads from the snapshot, not from the current players.handicap_index.
- **Finalized rounds are immutable.** Once a round has is_complete = true, its stored CH and snapshot HI never change. The LT1 self-heal is gated on is_complete = false.
- **Active rounds auto-cascade.** When admin edits a player's HI in the Players tab, the snapshot is automatically updated for every round_players row where the player is currently in an in-progress round (is_complete = false). This handles both the correction case ("I forgot to update before tee-off") and the mid-round-update case. Finalized rounds untouched.
- **No mid-round affordance needed.** Admin doesn't need a separate "apply to current round" button. The HI edit IS the application.
- **Historical import uses the same column.** Phase H.5 importer writes the per-round HI Dad provides directly to handicap_index_snapshot. No special path; same column, same math.
- **Backfill: copy current HI to snapshot.** Existing round_players rows pre-H.2.5 had their CH computed from the player's then-current HI, which happens to equal current HI for all existing prod rounds (no HIs have changed yet). Backfill is therefore lossless.

---

## Session Log

*High-level milestones for context.*

| Date | What got done |
| --- | --- |
| Apr 21 | Phase 0 — accounts, tools, deploy infrastructure |
| Apr 21–27 | Schema, seed data, core app with Gemini |
| Apr 27 (eve) | Tee/CH bugs, tap-to-select roster, team scoring, leaderboard, admin toggles, round summary |
| May 1 | Updated rosters, mobile redesign, dangerous-action pattern, played-with v1, history tab v1 |
| May 5 | Major feedback consolidation. Locked decisions on game format engine, blind draw, leaderboard rework, history/betting split, played-with redesign. Rules doc + historical data spreadsheet drafted. Roadmap rebuilt. Fixed A7 home-page team-card names — follow-up: same Supabase array-vs-object pattern likely affects History.tsx, scorecard/page.tsx, summary/page.tsx; needs its own ticket. Fixed navigation trap on empty/abandoned rounds in admin RoundSetup. Follow-up: RoundSetup useEffect dep on `allPlayers` reference re-runs `loadRoundForDate` unnecessarily when parent re-creates the array — minor perf, separate ticket. Follow-up: `goToTeams` does delete-then-insert on `round_players` without a transaction — failed insert loses all team assignments, separate ticket. Shipped Phase A scorecard UI cleanup: A1, A2, A3, A4, A5, A6, A8. Follow-up A4-extended: leftover CH/HC/HCP instances on leaderboard, round/new, summary, and admin RoundSetup tab — separate ticket. A2 revised: top pill now shows only the net delta (e.g., `−3`, `+2`, `E`) at 2rem; absolute net number and parentheses removed. Shipped Phase B1: rounds.format / rounds.format_config / rounds.format_locked_at + CHECK constraint + 2-Ball backfill. SQL committed at supabase/migrations/001_phase_b1_rounds_format_columns.sql. Note: format-picker UI not yet shipped — DEFAULT '2_ball' on the format column keeps the current round-creation insert flow working until the picker UI lands in a later B-phase step. Follow-up logged as I11 (type-safety via `supabase gen types`). Shipped B1.5: Vitest testing infrastructure (vitest.config.ts, tests/ directory, npm test + test:watch scripts). Pure-Node environment, no DOM yet. Setup-verification test passes. B2's scoring engine will populate tests/lib/scoring/. Shipped B2: extracted scoring engine to src/lib/scoring/ (types, handicap, engine + dispatcher, index). 2-Ball implementation only; B3-B5 will fill other format branches via the dispatcher. Rewired scorecard, summary, leaderboard, round/new to call the engine. 25 unit tests passing. Snapshot script tests/snapshots/snapshot-b2.mjs (run via `npm run snapshot:b2`) compared engine vs legacy inline math on all live rounds (36/36 match) before and after rewire. Added tsx as devDep for running TS scripts. Marks B4.5 ✅. Shipped B3: 3-Ball format added to scoring engine via the dispatcher. Renamed compute2BallHole → computeBestNHole; new defaultBestN(format) helper provides format-aware fallback (2 for 2_ball, 3 for 3_ball). 11 new unit tests (9 in engine-3ball.test.ts, 2 in engine-bestn.test.ts); 38 tests total. New snapshot script tests/snapshots/snapshot-b3.mjs (npm run snapshot:b3) does both live-data 2-Ball regression check (36/36 match — confirms the rename + default fallback didn't drift) and synthetic 3-Ball assertions. Marks B2.2 ✅. Shipped B4: Stableford Standard, Stableford Modified, and GOBS House formats added to the engine via three dispatcher branches sharing a single `computeStablefordHole(input, table)` helper. New `getStablefordPoints` net-vs-par-delta lookup (exported), Standard/GOBS House point tables as constants, `mergePointTable` overlays format_config.point_values for Modified. API expansion: added `points: number | null` to PlayerHoleResult so display code reads team and per-player points without re-implementing the lookup. computeRoundResult gates the bestN calc on isBestN; teamParAtScored stays at 0 for points-based formats. 22 new unit tests (engine-stableford.test.ts); 60 tests total. New snapshot script tests/snapshots/snapshot-b4.mjs (npm run snapshot:b4) covers live-data 2-Ball regression + synthetic Standard + Modified (custom point_values) + GOBS House (negative totals). Marks B2.3, B2.4, B2.5 ✅. Engine still throws for unimplemented formats — none remain in Phase B; B5 adds per-hole "all scores count" overrides on top of existing formats. B5 engine: per-hole override logic implemented in scoring engine. UI for admin to set format_config.override_holes ships in B6 (B3.1 multi-select, B3.2 net/gross toggle, B3.3 override banner) — table status for B3.x stays 📋 until then. computeBestNHole now branches on isOverrideHole: override sums all non-null contributors (best-all), wins over manualContributors. computeRoundResult.teamParAtScored scales by actual contributor count so override holes accrue par×N. Stableford / GOBS House are documented no-ops for overrides since they already sum every player. 13 new tests (engine-overrides.test.ts); 73 tests total. New snapshot tests/snapshots/snapshot-b5.mjs (npm run snapshot:b5) does live-data 2-Ball regression + synthetic 2-Ball with overrides on holes 9/18 + GOBS House override no-op verification. |
| May 6 | Shipped B1.2 + B1.3 + B1.4 + B1.5: format gate UI. New `src/components/format/` (FormatPicker, FormatNotSetBanner, ScorecardLockNotice). New `src/lib/format/` (copy.ts with FORMAT_LABELS + DEFAULT_FORMAT_CONFIG, helpers.ts with `roundNeedsFormat` + `defaultConfigFor`). New `src/lib/useIsMobile.ts` lifted from RoundSetup. Migration `supabase/migrations/002_phase_b1_drop_format_default.sql` drops `DEFAULT '2_ball'` and `NOT NULL` on `rounds.format`. Round-creation INSERT paths in `src/app/admin/tabs/RoundSetup.tsx` (`createRound`) and `src/app/round/new/page.tsx` (`startRound`) now send explicit `format: null, format_config: null`. Banner surfaces on homepage (today's round only) and admin RoundSetup active view. Scorecard renders `<ScorecardLockNotice/>` early when `rounds.format` is null and round is not complete — replaces the entire scorecard chrome (no setup, no +/−). Picker is bottom sheet on mobile (<768px) and centered modal on desktop. Lock copy uses generic "admin" label. Stableford Modified currently uses standard point values; per-round edit UI deferred (logged as TD7). 10 new tests (helpers.test.ts), 120 tests total; all four snapshots still pass. Tech debt added: TD6 (hardcoded `format: "2_ball"` in scorecard/summary engine calls), TD7 (Stableford Modified edit UI). Migration must be applied to Supabase before banner / lock states are reachable in production — gate-state screenshots deferred until apply confirmed. |
| May 7 | **Phase B.1 complete.** Shipped B1.6: format locks at first score, dangerous-action modal for post-lock change, plus TD6 hardcode cleanup. New `FormatChip` component (read-only or editable via `onChange` prop) wired into admin RoundSetup. `FormatPicker` accepts `currentFormat` prop and highlights the existing pick. Score-write path in `scorecard/page.tsx` `setScore` now calls `ensureFormatLocked()` after a successful insert/update; idempotent via local short-circuit (`roundFormatLockedAt` state) plus DB-side `WHERE format_locked_at IS NULL` guard. Helper `isFormatLocked` added to `src/lib/format/helpers.ts`. Scorecard and summary engine calls now read `rounds.format` and `rounds.format_config` dynamically (TD6 ✅ resolved). Editable chip on `/admin` opens `DangerModal` ("Change format mid-round?" / "Scores will be re-totaled under the new format.") before the picker when locked; opens picker directly when unlocked. `format_locked_at` is **not** changed when admin swaps format — semantically still "first score entered at this timestamp." 2 new helper tests; 122 total. All four snapshots pass. No new migrations required (B4.3 already added the column nullable). |
| May 8 (afternoon) | **Milestone rollup ahead of first live golf-course test.** Phase B fully shipped (B3.1 + B3.2 + B3.3 landed May 7 late). Phase C PR 1 (C3, format-aware team total via `formatTeamTotal` helper) and PR 2 (C1 + C2, live team leaderboard rebuild + `/leaderboard` ↔ `/season` route shuffle) both shipped. Terminology lockdown: new `### Terminology` subsection at the top of Decisions Locked codifies Handicap Index (HI, on `players.handicap_index`) vs Course Handicap (CH, on `round_players.course_handicap` — the May 8 afternoon entry initially named a non-existent `player_course_handicaps` table; corrected 2026-05-09); UI display rule "no bare 'Strokes' or 'Handicap' alone" applied across scorecard, admin Players tab, player profile, and the player-facing players list. Players-list cleanup: per-row "Strokes" labels removed, single right-aligned "Handicap Index" column header added at top of list (matches small-caps muted-gray section header pattern used elsewhere). Tech debt logged this run: TD11 (snapshot scripts need format-filter guard in live-data regression loop), TD12 (FormatPicker now requires explicit Save even for fresh rounds — quick-save affordance deferred), TD13 (`show_leaderboard` admin toggle now gates `/season` rather than the new live `/leaderboard` after the route rename). All TDs sized as Low–Medium, none launch-blocking. **Live golf-course testing started today** — feedback expected to drive the next session's priorities. PR 3 (C4 + C5 + C6, drill-in summary with F9 / B9 / Total) is next up and will incorporate live-test feedback. No code changes in this entry — ROADMAP-only housekeeping pass. |
| May 8 (label clarity) | Pre-live-test label sweep. Database fields stay (`handicap_index` on `players`, `course_handicap` on `round_players` — earlier draft of this entry referenced a non-existent `player_course_handicaps` table; corrected 2026-05-09 alongside the LT1 fix); UI labels disambiguated ahead of first golf-course test. **Scorecard (`src/app/round/[id]/scorecard/page.tsx`):** tee-selection card header `Strokes` → `Course Handicap`; no-HI inline-input placeholder `Enter Strokes index` → `Enter Handicap Index`; per-player row metadata strip `Strokes: N` → `Course Handicap: N · Handicap Index: M.M` (decimal preserved via `.toFixed(1)`; `handicap_index` was already in the scorecard fetch shape so no query change needed). Container has `flexWrap: "wrap"` so the longer label gracefully wraps to a second line on narrow phones. **Admin Players (`src/app/admin/tabs/Players.tsx`):** new-player form input placeholder `Handicap` → `Handicap Index`; desktop column header `Handicap` → `Handicap Index`; mobile card meta line `Strokes ${HI}` / `No Strokes` → `Handicap Index: ${HI}` / `No Handicap Index`; "No Strokes" amber pill on desktop → `Not on file` (matches existing scorecard tee-selection copy); inline action buttons (mobile + desktop) `Add Strokes` / `Edit Strokes` → `Add HI` / `Edit HI` (compact form for tight 160px desktop action cell and narrow mobile right-column). **Player profile (`src/app/player/[id]/page.tsx`):** per-round meta `Strokes: ${course_handicap}` → `Course Handicap: ${course_handicap}`. Profile header at line 153 already read `Handicap Index` correctly — left alone. **Decisions Locked:** new `### Terminology` subsection at the top of Decisions Locked codifies HI vs CH and the "no bare Strokes/Handicap" display rule. Old "Strokes terminology" Phase A bullet removed (superseded). "Strokes display" bullet renamed to "Stroke-allocation dots" to clarify it's about the per-hole stroke indicator, not the disallowed bare label. **Option C chosen for the scorecard player row:** show both Course Handicap and Handicap Index side-by-side. Reasoning: during early league use the league members are still building intuition about the two quantities, and surfacing both makes the slope adjustment visible per round. May revert to Option A (CH only on scorecard) once the league has settled if the row reads cluttered in real-world use. No math changes, no engine changes, no schema changes. 151/151 tests pass; all four snapshots clean; `tsc --noEmit` clean. |
| May 10 (evening) — tee default + per-player preference | **Pre-selection improvement on the scorecard Tee Selection screen + new per-player tee preference column.** Background: league plays White/Yellow Combo ~99% of the time; current Tee Selection picker opens with nothing selected and admins/players have to tap a tee per player on every new round. Wayne Vincent is the lone known exception today (always plays White), with more likely later. **Migration `004_phase_a_preferred_tee.sql`:** adds `players.preferred_tee_id integer NULL REFERENCES tees(id) ON DELETE SET NULL`; seeds Wayne Vincent (players.id=55) to White (tees.id=2). **New `src/lib/tees.ts`:** exports `DEFAULT_TEE_ID = 4` (White/Yellow Combo) with a comment noting the hardcode rationale and the seed sync requirement. **Insert paths unchanged:** `toggleInRoster` and `goToTeams` in `RoundSetup.tsx` still insert `tee_id: null`. The `needsSetup` gate on scorecard stays as-is, so every new round routes through the Tee Selection picker — Jonathan explicitly confirmed this UX must continue. **Pre-selection in the picker:** scorecard's Tee Selection screen now computes `effectiveTeeId = rp.tee_id ?? rp.preferred_tee_id ?? DEFAULT_TEE_ID` for the per-player tee button `isSelected` check, so the right tee opens pre-highlighted. Player/admin can override with a tap or just hit START ROUND to confirm. **START ROUND bulk-commit:** the button now iterates over `roundPlayers` and calls `updatePlayerTee(rp.id, rp.preferred_tee_id ?? DEFAULT_TEE_ID)` for any row whose `tee_id` is still null, then flips `needsSetup` off. Replaces the prior `alert("Please select a tee for every player")` block — confirming a pre-filled picker is now one tap. **`/round/new`** picker also fixed: `togglePlayer` now sets `tee_id: player.preferred_tee_id ?? DEFAULT_TEE_ID` per-player instead of the global `defaultTeeId` state (which had been seeded to the first tee in sort order — Blue — i.e. the wrong league default). Dropped the now-unused `defaultTeeId` state. **Embedded joins updated:** `RoundSetup.loadRoundForDate` and `scorecard/page.tsx` load both pull `preferred_tee_id` from the `players (...)` join; types updated. **New Phase I items:** I13 (📋 admin UI to edit `preferred_tee_id`), I14 (💡 mid-round tee change UI — deferred per spec). **Verification:** `tsc --noEmit` clean; 164/164 unit tests pass; browser preview confirms admin and homepage render without console / server errors. Migration applied separately by Jonathan against prod. Live runtime verification on next round. |
| May 10 (evening) — stale format on round delete | **Deleting a round didn't refresh the Today's Format display.** `doDeleteRound` was manually resetting `existingRoundId`, `isRoundComplete`, `roster`, `teams`, `viewMode` — but missed `roundFormat` / `roundFormatConfig` / `roundFormatLockedAt`. After delete the format strip kept rendering the deleted round's format (e.g. "2-Ball") instead of reverting to the empty "Pick today's format" nudge. **Fix:** replaced the manual setState block in `doDeleteRound` with `await loadRoundForDate(selectedDate)`. That's the canonical "reset to today's true state" path — it clears every piece of round-derived state up front, then re-populates from a fresh DB read. Single source of truth, no drift between delete and load. **Also patched `ensureRoundShell`:** explicitly resets `roundFormat`/`roundFormatConfig`/`roundFormatLockedAt` at the same point it sets the new round id. Closes the latent in-place "delete round → tap Edit Teams" path where the shell would land with stale format values cached in React. **Verification:** `tsc --noEmit` clean; 164/164 unit tests pass; browser preview renders admin tab without errors. Live runtime verification deferred to Jonathan's next test (delete is destructive against prod DB — won't drive end-to-end here). |
| May 10 (evening) — checkin minimum gate | **Hardcoded 4-player minimum on the mobile checkin → teams transition blocked the common case.** League plays in 2s, 3s, or 4s, so the 4-min gate forced admins to check in placeholder players to advance. Bug only bit on the fresh-day flow because post-creation Edit Teams loads existing rostered players into `roster` state via `loadRoundForDate`, so `roster.length < 4` was rarely true in practice. Fix: replaced the hardcoded `roster.length < 4` ternary in `RoundSetup.tsx`'s mobile checkin CTA with a 1-player floor — `"Check in at least one player"` (disabled) when roster is empty, `"Assign to teams → (N players)"` (enabled) otherwise. Single edit, no other lines touched. No max-per-team check existed elsewhere; nothing to preserve on the upper bound. Desktop edit mode renders both check-in and team-assignment on one screen and has no gate. **Verification:** `tsc --noEmit` clean; 164/164 unit tests pass; browser preview confirms enabled-with-1, disabled-with-0. |
| May 10 (evening) — date timezone bug | **UTC-vs-local mismatch on "today" computations.** Jonathan's evening live test: admin RoundSetup created scorecards for May 10 (local Pacific) correctly. Tapping Start a Scorecard from `/` then created scorecards for May 11 — two `played_on` values for one league day. **Root cause:** admin's date picker default used local-date construction (`getFullYear/getMonth/getDate`) while every other "today" callsite used `new Date().toISOString().split("T")[0]` — i.e., UTC date. After ~5 PM Pacific, UTC has rolled over, so the homepage path resolved to tomorrow. Same bug also explains why some evening "today" lookups appear to find nothing — read filters in `page.tsx`, `leaderboard/page.tsx`, and `round/active/page.tsx` were all UTC-based and would miss the local-today round after rollover. **Fix:** new shared helper `src/lib/date.ts` exporting `todayLocal()` and `yesterdayLocal()`, both returning local-timezone YYYY-MM-DD. Routed every "today's date for `rounds.played_on`" callsite through it: `page.tsx` (homepage rounds read + isYesterday comparison), `round/new/page.tsx` (existing-round lookup + insert), `round/active/page.tsx` (active round lookup), `leaderboard/page.tsx` (today filter), `admin/tabs/RoundSetup.tsx` (date picker default). Helper has a comment calling out the single-region intent and flagging the "remote player in another timezone" caveat as a future revisit. **Format-only `toLocaleDateString` callsites** (History, players, player profile, scorecard, summary, RoundSetup formatDate, leaderboard prettyDate) untouched — they parse stored YYYY-MM-DD and were never affected. `format_locked_at` is a UTC timestamptz column, untouched. **Tonight's stray May 11 row** left for Jonathan's manual cleanup (or natural expiry after midnight Pacific). **Verification:** `tsc --noEmit` clean; 164/164 unit tests pass; browser preview confirms no console / server errors. Live runtime verification after 5 PM Pacific deferred to Jonathan's next test. |
| May 10 (admin read + write-race fix) | **Live-test bugs from the flow pivot rollout: admin RoundSetup not rendering teams.** Three symptoms: (1) fresh "check in → drag to team → Done" produced no teams in admin view; (2) scorecards visible on `/` did not appear in admin; (3) existing teams rendered as empty drop slots. **Two pre-existing bugs unmasked by the new entry flow.** **Bug A — silent inactive-player drop in admin read path:** `loadRoundForDate` resolved player metadata by `allPlayers.find(p => p.id === rp.player_id)` against an active-only prop from the parent. Any player rostered for the round who'd later been deactivated in the Players tab was silently dropped from the admin view, while the homepage (which uses an embedded join) rendered them fine. **Upstream cause:** `Players.tsx` deactivate flow is a single tap + confirm modal with **no check that the player is in a live round** — so an admin can deactivate a rostered player. Not addressed here (separate concern); fix is to make the display robust to this state. **Bug B — fire-and-forget write race:** `toggleInRoster` INSERT and `assignToTeam → autosaveAssignment` UPDATE were both fire-and-forget. Fast tap pattern (check player in → assign to team) could fire UPDATE before INSERT landed → UPDATE matched 0 rows silently → team_number stayed 0 → reload showed no team. Pre-TD4 `goToTeams`'s delete-all-then-insert-all happened to mask part of this; the diff-based reconciliation doesn't. **Read fix:** `loadRoundForDate` switched to the embedded join `players ( id, full_name, display_name, handicap_index, is_active )` with the standard `Array.isArray(rp.players) ? rp.players[0] : rp.players` guard, building the player object from the joined row. `allPlayers` prop still drives the check-in list (legitimately active-only). Inactive players in `round_players` now render in the admin view — no badge, no filter, no special treatment. **Write fix:** new per-player write queue (`writeQueueRef: useRef<Map<number, Promise>>` + `enqueueWrite(playerId, fn)` helper). `toggleInRoster` INSERT/DELETE and `autosaveAssignment` UPDATE both route through it, so writes for the same player_id serialize while cross-player writes still run in parallel. `goToTeams` and `doneEditing` both call `drainWrites()` before their reads / reload to eliminate stale-state windows. **Bonus:** History.tsx had the same TD2 array-vs-object pattern without the guard (would silently render "?" for player names if PostgREST returned arrays) — fixed defensively in the same commit. **TD2 ✅** — last load-bearing instance resolved. **Verification:** `tsc --noEmit` clean. 164/164 unit tests pass. Browser preview confirms no console / server errors. Live runtime verification (deactivated-player visibility + race-free check-in → assign → Done cycle) deferred to Jonathan's next test. |
| May 10 (flow pivot) | **Pivot on admin format/team-build flow + TD4 fix.** Driven by May 10 live behavior review with Jonathan. Three issues bundled: (1) admin team-build "Done" was silently failing (TD4 — `goToTeams` non-transactional delete-then-insert); (2) format-not-set UI was leaking onto homepage `/`; (3) format and team-building needed to be settable independently before any scorecards exist. **`src/app/admin/tabs/RoundSetup.tsx`:** dropped the empty-state "+ Create today's round" CTA, the `FormatNotSetBanner` render, the `FormatChip` render, and the bottom Edit-teams button. All collapse into a new top-section pair of buttons rendered in both empty (`viewMode === "none"`) and populated (`viewMode === "active"`) states: "Today's Format" (yellow nudge when `roundFormat === null`, neutral chip + lock-icon when set) and "Edit Teams". Either button calls new helper `ensureRoundShell()` which inserts a `rounds` row (`played_on, course_id: 1, format: null, format_config: null`) if none exists for the selected date, then opens its respective flow. Players starting from `/round/new` already auto-create shells the same way (unchanged). **`src/app/page.tsx`:** dropped `FormatNotSetBanner` import + render, `roundNeedsFormat` import + computation, and the `format` column from the rounds select. Homepage no longer renders any format UI in any state. `ScorecardLockNotice` on the locked scorecard view remains the only player-visible format-state surface. **`src/components/format/FormatNotSetBanner.tsx`:** deleted (no remaining callers). `FormatChip.tsx` kept — still used as the read-only chip on the player scorecard header. **TD4 fix in `goToTeams`:** replaced delete-all-then-reinsert with a diff-based reconciliation. Reads existing `round_players`, computes `toRemove = existing.player_ids \ roster.player_ids` and `toAdd = roster.player_ids \ existing.player_ids`, then deletes only `toRemove` and inserts only `toAdd`. Players present in both sets are never touched, so `team_number` survives any partial failure structurally — no transaction / RPC needed. **Decisions Locked rewrite (not amendment):** "Format selection entry point" entry rewritten to reflect the two-button flow; previous 2026-05-09 wording superseded entirely rather than annotated. Discussed and approved with Jonathan rather than accumulating amendment notes. **Verification:** `tsc --noEmit` clean. Existing unit tests + format-helper tests pass; no engine math touched. Manual verification of the six scenarios (Today's-Format-first shell create, Edit-Teams-first shell create, `/round/new` shell create, post-team format-set unlocks scorecards live, partial-save Done preserves teams, homepage stays format-UI-free) requires live Supabase and was deferred to the Monday round. Commit lands on master. Hotfix: shell insert needed non-null `format_config` (DB has NOT NULL constraint) — added `DEFAULT_FORMAT_CONFIG_SHELL = { basis: "net", scoring_basis: "net", override_holes: [] }` to `src/lib/format/copy.ts`, picker's `commitSave` overwrites the whole config on first format selection. Redundant "Pick" label removed from the Today's Format button's right-side affordance so it matches Edit Teams' arrow-only style. |
| May 10 (LT2 triage) | LT2 triage on iPhone with Web Inspector against `lt2-repro` (instrumentation live). Ran 3 variants: Variant A (single player, in-app Back), Variant A2 (alternate nav method), Variant B (3 players, 12 steps including rapid nav stress). **All CLEAN** — no score reversion, no phantom `setScore` on nav, cumulative pill correct throughout. Of the 3 candidate mechanisms going in, score-overwrite and dual-bug-cumulative+persistence are ruled out for these scenarios. **Remaining theory:** Dad in-the-moment misread during the live round, or a condition not yet hit (specific format / team size / network race). LT2 stays 📋 — paused pending observation in next live round with instrumentation still deployed; not blocking other work. **A1.6 / A1.7 unblocked** — score-entry surface no longer treated as paused. Header line updated to reflect new state. ROADMAP-and-STATUS-only commits; no code changes. |
| May 10 (merged) | Phase A.1 PR 1 merged to master and verified live on production. A1.1–A1.5 + TD7/TD10/TD11/TD13 all shipped. A1.6 / A1.7 still deferred pending LT2 fix. |
| May 10 | **Phase A.1 PR 1** on `phase-a1-stableford-best-ball-format-picker`. Five chunks bundled. **A1.4 — Drop GOBS House:** removed enum value, dispatcher branch, `GOBS_HOUSE_POINTS`, FORMAT_LABELS / FORMAT_ORDER / DEFAULT_FORMAT_CONFIG entry, picker `STABLEFORD_FORMATS` member, leaderboard rank `STABLEFORD_FORMATS` member, summary Stableford gate, all unit/snapshot test coverage. Pre-migration check (anon SELECT on `rounds.format`): zero `gobs_house` or `stableford_modified` rows in prod (2 total rounds, both `2_ball`). **A1.1 + A1.5 — Rename + new tables + editable UI:** engine `stableford_modified` → `gobs_stableford` throughout; `STABLEFORD_STANDARD_POINTS` updated to the new locked table (Albatross +8, Eagle +5, Birdie +3, Par +2, Bogey +1, DB+ 0); new `GOBS_STABLEFORD_POINTS` constant (Albatross +8, Eagle +5, Birdie +3, Par +2, Bogey 0, DB+ −1). `getStablefordPoints` verified to collapse any delta ≥ +2 into the DB+ bucket. FormatPicker gains a "Point values (per round)" section that renders only when GOBS Stableford is selected: 6 number inputs (Albatross / Eagle / Birdie / Par / Bogey / DB+ or worse), clamped to [−10, +10], with a "Reset to defaults" link. Editable values persist to `format_config.point_values` and the engine reads them via `mergePointTable(GOBS_STABLEFORD_POINTS, …)`. Mid-round edits still route through the existing FormatPicker DangerModal (already wired for any `format_config` change post-lock). Resolves TD7. **A1.2 — Best Ball:** added enum value `best_ball`, `defaultBestN("best_ball") === 1`, dispatcher routes through `computeBestNHole`, `isBestN` includes `best_ball` for the `teamParAtScored` path (par × 1 × holes). FormatPicker: Net/Gross toggle disabled with "Best Ball is always net" caption + `useEffect` that force-flips local state to "net" so a stale "gross" choice can't slip through commit. Best Ball is a best-N family format → override-holes apply normally (override = sum all non-null contributors). Scorecard `isBestNFormat` updated to include `best_ball`. New unit test file `engine-best-ball.test.ts` (7 tests) + new snapshot script `snapshot:b6` covering single-winner, handicap-flip, tie ordering, override hole, round teamParAtScored, all-null hole. **A1.3 — Format picker placement:** the decoupling already held in code (FormatNotSetBanner on admin RoundSetup, FormatChip on /admin for post-lock edits, team-build flow not gated on format). State machine semantics codified in ROADMAP A1.3 row: format choice and scorecard unlock are decoupled — scorecard view still shows `ScorecardLockNotice` while `roundFormat == null` (LT2-paused score-entry zone, left intact). **TD13 — leaderboard gate:** Settings UI relabeled from "Show Leaderboard / Visible on the public leaderboard page" to "Show Season Stats / Visible on the season stats page (/season). Live leaderboard always visible." DB key `show_leaderboard` left unchanged (per user instruction to avoid the rename migration). `/leaderboard` already publicly accessible; `/season` gate logic unchanged. **TD10 — stale 2-ball toggle:** removed Toggle + entire "Scoring" Card from `Settings.tsx`, dropped `two_ball_scoring` from `ToggleKey` and from the `defaults` seed in `admin/page.tsx`. Existing rows in `league_settings` left untouched (UI just stops surfacing the key). **TD11 — snapshot filter guard:** added `if (round.format && round.format !== "2_ball") continue;` to the Part 1 loop in `snapshot-b2`, `snapshot-b3`, `snapshot-b4`, `snapshot-b5`, and the new `snapshot-b6`. **Migration `003_phase_a1_format_set_rebalance.sql`:** drops the old CHECK, renames any `stableford_modified` rows to `gobs_stableford`, re-adds CHECK with new enum (`2_ball`, `3_ball`, `best_ball`, `stableford_standard`, `gobs_stableford`). Includes rollback statement in the file header. **Verification:** `tsc --noEmit` clean. **164/164 unit tests pass.** All 5 snapshots clean (b2/b3/b4/b5/b6). **Out of scope / deferred:** A1.6 (F9/B9/Total pill) and A1.7 (tap player row → expand) — both sit on the live score-entry / hole-navigation surface that's paused while LT2 is reproduced on Dad's iPhone. They go in a follow-up PR once LT2 is fixed. **Branch:** awaiting user review; do not merge until confirmed. |
| May 9 | First live-course feedback session. Phone consultation with Dad covering the May 8 round. **Two critical bugs identified:** scorecard CH displaying wrong values vs DB (Kevin/Wayne examples confirmed live), and scores reverting to par on hole navigation (reproduced by two testers same round). New **Phase 0.5** created for these — investigation + fixes ship before any other phase work, with Vercel preview deployed for Dad to verify on his phone before merging to master. **Format set rebalanced:** Stableford Modified and GOBS House dropped from codebase, format enum, UI, tests, snapshots, and roadmap. **GOBS Stableford** added as a new format with league-specific point table (Albatross/Eagle/Birdie/Par/Bogey/DB+ = +8/+5/+2/0/−1/−2). **Stableford Standard** retains canonical USGA values (Bogey 1, Par 2, Birdie 3, Eagle 4, Albatross 5, DB+ 0); verified against current code as part of A1.1, expected to ship as a no-fix verification. **Best Ball** added as the 5th format, locked to net-only with strict best-1 selection regardless of team size; net/gross toggle disabled in picker; override-holes a documented no-op. **Format picker entry point** moved from scorecard-gated to admin Round Setup tab to match Dad's actual workflow (admin picks format before pairings are drawn). **Blind Draw work (Phase D.1) reprioritized ahead of Phase C PR 3** per Dad's request — blind draw applies roughly every other round due to typical odd-player counts, needs real-round testing. **Phase D.2 (rainout) deleted** — league rule: if play stops, no payouts, round doesn't count, no app-side partial-round handling. **Phase H.4 deleted** (dependent on D.2). **Open Question Q8 deleted** (answered by deletion of D.2). **Phase H.2 (DB backup) elevated:** now also blocks historical data import — Dad wants to enter 2026 historical rounds and needs partial-reset workflow first. New scorecard items in Phase A.1: F9/B9/Total on team-net pill (drives Nassau bet payouts), tap-player-row expand hole-by-hole. New Phase H deliverables: H6 QR code, H7 Add-to-Home-Screen instructions doc, H8 one-shot DB export for Dad's manual verification. **BFB fund visibility on home page** added to Phase F.2 (F2.8). New Decisions Locked: Stableford point values (Standard USGA + GOBS values), Best Ball spec, rainout cancellation, non-paying player handling, format selection entry point, format set (5 formats). TD7 (Stableford Modified edit UI) voided. **Active priority order (post-May 9):** Phase 0.5 → Phase A.1 → Phase D.1 → Phase H.2 → Phase C PR 3 → Phase E onward. Roadmap-only commit; investigation diagnostics for LT1 + LT2 and subsequent fixes follow as separate commits. |
| May 8 | Phase C, PR 2 — C1 + C2: live team leaderboard rebuild. **Route shuffle:** `git mv`'d `src/app/leaderboard/page.tsx` → `src/app/season/page.tsx` verbatim (preserves the existing per-player season stats page exactly as-is, including the `show_leaderboard` admin gate behavior). Bottom nav in `layout.tsx` unchanged — still routes to `/leaderboard`, now the team view. **New page:** `src/app/leaderboard/page.tsx`. Four state branches: (a) `no_round` — no row in `rounds` for today's date; (b) `no_format` — round exists but `format` is null; (c) `live` — round exists with format, `is_complete` false; (d) `complete` — `is_complete` true. States (a) and (b) render the same dashed-border empty card + "View season stats →" link to `/season`; the only differences are the in-page navy state strip's subtitle ("No round today" vs "Round in progress") and whether the format chip appears below the date. **Engine reuse:** scoring-engine `computeRoundResult` called once per team with `getScoringBasis` → `useGross` → zero-handicap trick already established in scorecard/summary. Display value passed to `formatTeamTotal` is `teamScore - teamPar`, which collapses to absolute team points for Stableford (engine returns `teamPar = 0` there). **Color coding:** Stableford-family score → blue (`#2563eb`) regardless of sign; best-N score → green (under par) / red (over par) / black (even). 1st-place rank circle is `#d4a017` gold, others navy. **Pure helpers** in new `src/lib/leaderboard/rank.ts`: `rankTeams<T>(teams, format)` (decorate-sort-undecorate, format-aware direction, stable on ties, "skip" tie-rank semantics) and `holesCompleteForTeam(scoresByPlayer, requiredPlayerIds)` ("thru N" = hole counts only when every required player has a non-null score). Both pure, no DB. **Tests:** 13 new in `tests/lib/leaderboard/rank.test.ts` covering best-N ascending, Stableford descending, two-team tie at 1st (next is rank 3), three-way tie, all-tied, single team, GOBS House negatives, immutability, plus three thru-N edge cases (basic, empty required list, null/undefined treated identically). **Out of scope deliberately:** state-aware GLOBAL app-header subtitle from the mockup; instead, an in-page navy strip on the leaderboard page only. The global header in `layout.tsx` stays "Semiahmoo Golf & Country Club" — a per-page dynamic subtitle would require a layout-level prop drill or context. Easy follow-up if Dad wants. **Drill-in routing:** rows tap through to existing `/round/[id]/summary` page; PR 3 rebuilds that. **Verification:** 151/151 unit tests pass; all four snapshots clean (no engine math touched); `tsc --noEmit` clean. **Tech debt logged:** TD13 — the `show_leaderboard` admin setting now gates the relocated `/season` page rather than the new live `/leaderboard`; label intent no longer matches; resolve during Settings tab polish (alongside TD10). |
| May 7 (night) | Phase C, PR 1 — C3: Format-aware team total display. New `formatTeamTotal(total, format)` helper in `src/lib/format/copy.ts`. Best-N input is interpreted as a stroke delta vs par → `+N` / `−N` / `E`. Stableford-family input is interpreted as absolute team points → `${total} pts`. Both branches use Unicode minus (U+2212) for negatives — matters for GOBS House where `−1` deductions can drop a team's points total below zero. **Sites swapped:** scorecard team net pill (`src/app/round/[id]/scorecard/page.tsx:617`) — passes `teamNet - teamPar`; for Stableford, `teamPar` (= `teamParAtScored`) is 0 by engine contract, so the delta naturally collapses to the absolute points total and the helper's Stableford branch renders correctly. Round summary (`src/app/round/[id]/summary/page.tsx:281`) — gated branch: Stableford-family routes through helper for "X pts"; best-N keeps the existing raw absolute display (e.g. `37` not `+5`) since the summary never used the delta convention pre-PR. **Sites NOT swapped:** `src/app/leaderboard/page.tsx` is the season-level individual leaderboard, no team totals — no swap; live in-round team leaderboard belongs to C1/C2 which are still 📋. Player profile's `scoreLabel` is per-player vs par 72 — explicitly out of scope per user. **Tests:** 5 new in new file `tests/lib/format/copy.test.ts` covering 2_ball positive/negative/zero, stableford_standard positive, gobs_house negative. **138/138 tests pass; all four snapshots clean** (no engine math touched). C1, C2, C4–C6 still 📋. Note: the helper has asymmetric input semantics (delta for best-N, absolute for Stableford); the contract is documented inline at the helper's definition. **Phase C follow-up logged:** scorecard pill (delta) and summary (raw absolute) currently disagree on stroke convention; resolve uniformly to delta during PR 2/3 leaderboard/drill-in work — see Phase C section's follow-up note for the design call. |
| May 7 (late) | **Phase B fully complete.** Shipped B3.1 + B3.2 + B3.3: per-round override holes admin UI + persistent net/gross toggle + on-scorecard override banner. **Type / helpers:** `FormatConfig.scoring_basis?: "net" \| "gross"` added (optional for backward compat). New helpers in `src/lib/format/helpers.ts`: `getScoringBasis(config)` (defaults to "net" for null/undefined/missing-key configs) and `getOverrideHoles(config)` (defensive [] fallback). `DEFAULT_FORMAT_CONFIG` seeds `scoring_basis: "net"` for every format. **FormatPicker rewrite:** two-step flow — pick format → reveal Scoring basis segmented control (Net / Gross) + 6×3 hole grid for override_holes with "9 & 18" preset and "Clear all" utility → Save commits everything in a single update. Stableford / Stableford Modified / GOBS House render the override section at 0.55 opacity with an inline "(no effect on Stableford formats)" caption — admin can still tap, but engine treats override_holes as a no-op there. Save button disabled until something differs from current config. **Mid-round guard (Change 3):** moved out of FormatChip and into FormatPicker's Save click. Single DangerModal ("Change scoring rules mid-round?" / "Scores will be re-totaled under the new rules.") fires when `format_locked_at` is set AND any of (format, scoring_basis, override_holes) differ from saved. FormatChip simplified — taps now open the picker directly; the chip's old format-only DangerModal is gone. `format_locked_at` is **not** modified during a rules edit (semantics preserved). **Engine integration trick:** zero-handicap-on-gross applied at both call sites (`src/app/round/[id]/scorecard/page.tsx` `computeHoleFor` + `buildRoundInput`, and `src/app/round/[id]/summary/page.tsx` `computeRoundResult` calls). When `getScoringBasis(config) === "gross"`, every player's `courseHandicap` is passed as 0; engine net pathway returns gross-equivalent values uniformly. Works for Stableford too (no engine change needed — Stableford has no internal `basis` branch). Note: this means Stableford summary's gross/net view toggle now shows divergent numbers on net-mode rounds (was identical pre-PR — the gross column was effectively dead) — a strict improvement, but a quiet behavior change for any Stableford round already in the DB. **Scorecard override banner (B3.3):** soft yellow banner ("All scores count on this hole") rendered between hole header and team-net pill when current hole is in override_holes. Gated on `isBestNFormat` since the override has no effect in Stableford and showing the banner there would mislead. Visible to all users (no admin gate). **Tests:** 11 new — 9 helper tests in `tests/lib/format/helpers.test.ts` (getScoringBasis, getOverrideHoles, scoring_basis seed in defaults), 2 engine tests in new file `tests/lib/scoring/engine-gross.test.ts` confirming zero-handicap collapses Stableford net to gross and 2-Ball gross == net under the same trick. **133/133 tests pass.** All four snapshots clean — round 47 (which was driving the 8 TD11 false-positives this morning) now produces no mismatches in any of b2/b3/b4/b5, suggesting it was either deleted or converted to 2_ball between sessions. TD11 stays open until the snapshot scripts get the format-filter guard regardless. No new migrations required (`scoring_basis` is optional and read with a "net" fallback). Out of scope / deferred: TD7 Stableford Modified per-round point-value edit UI; the summary page's gross/net view toggle UX in admin-gross-mode rounds (where both columns now show the same number — Phase C concern); cleanup of the legacy `FormatConfig.basis` field which is now a per-call display switch only and could be removed/renamed in a separate refactor. New tech debt logged: **TD12** — the picker's two-step flow adds friction to the previously one-tap fresh-round save path; add a "Quick Save" affordance only if real admin use surfaces the friction. |
| May 7 (PM) | Bundled scorecard polish: A6 behavior tweak, A9 (new), TD9 resolved. **A6** — `scorecard/page.tsx` +/− handlers in the player score row now branch on `current == null`: first tap on either button lands on `par` for the hole; subsequent taps increment/decrement normally. Previous behavior was `par + 1` / `par − 1` on first tap — confusing because the first tap of the round felt like an arbitrary stroke nudge. Tested: hole 2 par 4, fresh round, both buttons → 4. **A9** — added `isBestNFormat = format === "2_ball" \|\| format === "3_ball"` and gated four UI elements: per-player "BALL 1"/"BALL 2" badge, "Tied" badge, the tied-for-Ball banner, and the "tap a card to override which balls count" footer. In Stableford Standard / Modified / GOBS House every player's score contributes, so those affordances were misleading. Card border / "isCounting" highlight intentionally left in place — accurate in either format, just visually loud in Stableford; deferred unless feedback. Override-tap on cards still fires in Stableford but the engine treats `manualContributors` as a no-op for sum-all-players formats, so it's a silent dead click. **TD9** — read-only `FormatChip` rendered above "Hole N" in the scorecard header (centered, 10px below). Visible to all users from the moment a format is set. No `onChange` prop, so the chip is non-interactive on the player surface. `roundId` (string from `useParams`) coerced via `Number(roundId)` to satisfy `FormatChip`'s numeric prop. No scoring math touched; all 122 unit tests pass. Snapshot scripts (b2/b3/b4/b5) report 8 mismatches each — verified pre-existing (same 8 mismatches on a clean HEAD without these changes), all on round 47 played 2026-05-07 in a non-2-Ball format. Logged as TD11: snapshot scripts need a `if (round.format && round.format !== "2_ball") continue;` guard in the Part 1 live-data regression loop so non-2-Ball rounds stop hitting the legacy 2-Ball comparator. |
| May 20 (beta feedback sprint) | **Beta feedback sprint for Thursday May 21 live round. 8 commits to master.** Phase A.2 (player-driven team formation) shipped end-to-end: (1) `ensureRoundShell` lifted to `src/lib/round/` — shared entry point for all round-creation paths; (2) `smartJoin` pure logic + 9 unit tests (`src/lib/teamFormation/smartJoin.ts`) covering prefer-together, avoid-repeat, and mixed-team conflict branches; (3) `PlayerPickerSheet` component with two modes (self-join on homepage / admin-join on scorecard), mobile bottom-sheet + desktop popover; (4) Homepage integration (`src/app/page.tsx`) — "Today's teams" section wires the picker, per-player write queue, smart-join dispatch, `JoinTeamConfirmModal` (self-confirm before write), and `MixedTeamsErrorModal` (conflict guard); (5) Dedupe fix — `TodaysTeamsList` component deleted, "Today's teams" section was rendering twice; "Form a new team" folded into existing card rather than a separate section; (6) Manage Team button + sheet on scorecard (`src/app/round/[id]/scorecard/page.tsx`) — pre-scoring only, taps open reassignment flow, write queue pattern ported; (7) Blind-draw par display bug D1.12 fixed — `drawnPlayerPar` threaded through `results.ts` instead of hardcoded par-4; (8) `todayLocal`/`yesterdayLocal` date-mock test rot diagnosed and fixed in `tests/components/page-team-formation.test.tsx` (tests passed on commit day, failed every subsequent day — root cause: `vi.mock('@/lib/date')` not present). **Additional:** leaderboard per-team THRU N / FINAL caption (C2 update, commit `c89a504`) — `TeamCard` now accepts `isComplete` prop, renders "FINAL" on complete rounds, "THRU N" when in progress and `thru > 0`, "—" when thru = 0; old "Net pts" / "Net" caption removed. 5 new tests in new file `tests/components/round/RoundResultsView.test.tsx` (renderToString, no jsdom). **SQL:** Teams 7 + 8 on round 103 (test scorecards) deleted via Supabase SQL Editor by admin. **Tests:** 298 → 344/344. `tsc --noEmit` clean. **Tech debt logged:** TD17 (delete-scorecard affordance on admin RoundSetup), TD18 (extract `Player` type to `src/lib/types.ts`), TD19 (migrate `/round/new` to use lifted `ensureRoundShell`), TD20 (`withAdminFlags` in `admin.ts` exported but unused). **Locked patterns added to CLAUDE.md:** per-player write queue (existing pattern documented for the two new surfaces) + date-mock requirement for tests touching `todayLocal`/`yesterdayLocal`. |
| May 20 | **D1.11 + H1 shipped to master.** Replaces the D.1 DB-level finalize lock with a UI lock + admin edit mode, and folds in the `/thomas-admin` → `/admin` rename. **Migration `009_phase_d1_drop_scores_finalize_trigger.sql`:** drops trigger `scores_reject_on_complete` and function `reject_scores_on_complete_round` (note: actual function name has the `_round` suffix — the spec migration text had it wrong; verified against migration 008 line 75 before writing 009). **Client-side guard removed:** the `submitted_teams`-based abort + `lockedToastVisible` state + toast JSX deleted from `scorecard/page.tsx`; the per-team submit UI gate (`isLocked` from `myTeamSubmitted`) still hides +/− buttons on submitted teams, so the deleted abort was defense-in-depth. **New `src/lib/admin.ts`:** `useIsAdmin()` / `useIsRoundEditMode()` reactive hooks via `useSearchParams`, plus `enterRoundEditMode` / `exitRoundEditMode` / `buildSearchString` / `withAdminFlags` helpers. **New `src/components/round/EditModeBanner.tsx`:** yellow sticky banner with Done button, self-gates on `?admin=1&edit=1`. **New `src/app/round/[id]/layout.tsx`:** wraps summary + scorecard with the banner so edit mode persists across navigation. **`RoundResultsView.Header`:** "Edit Round Scores" button renders when round is finalized + `?admin=1`; tap → DangerModal ("Edit finalized round?" / "Blind draw assignments stay locked. Team totals will recalculate automatically as you edit." / Edit Scores button) → confirm navigates to `/round/[id]/scorecard?admin=1&edit=1`. **`scorecard/page.tsx`:** new `adminEditModeActive = isAdmin && isRoundEditMode && isRoundComplete` derivation; `isLocked` now `!adminEditModeActive && (isRoundComplete || myTeamSubmitted)` so admin edit mode bypasses both the finalize gate and (defensively) the per-team gate on finalized rounds. Live rounds unaffected. Blind-draw fill rows stay read-only — their score is computed from the source player's actual scores, so editing the source flows through automatically. **Rename:** `src/app/thomas-admin/` → `src/app/admin/` via `git mv` per-file (Windows folder rename hit a permission denied on the dev server's open handles); 4 DangerModal import paths + homepage Admin nav href + 11 doc references (CLAUDE.md + ROADMAP.md) updated via blanket `replace_all`. **Tests:** 26 new across 3 new files — `tests/lib/admin.test.tsx` (hooks + helpers), `tests/components/edit-mode-banner.test.tsx` (banner visibility + Done), `tests/components/admin-edit-flow.test.tsx` (summary header button + modal), `tests/components/admin-edit-scorecard.test.tsx` (scorecard read-only by default, +/− visible in admin edit, WriteQueue.enqueue called). Existing `submit-flow.test.tsx` + `scorecard-bug-repro.test.tsx` mocks updated to expose `useSearchParams` / `usePathname`. **272 → 298/298 passing**, `tsc --noEmit` clean. **WriteQueue P0001 classifier:** unchanged — kept as defensive scaffolding per spec; `getTerminalReason.test.ts` cases continue to pass. **Browser-verified:** the full flow exercised on a real finalized round (round 103) — Admin link routes to `/admin`, summary `?admin=1` shows the yellow Edit button, modal opens with correct copy + 1.5s delay, confirm navigates to scorecard with `?admin=1&edit=1`, banner pins, 18× +/− visible per team, Done drops `?edit=1` and the affordances unmount. Score writes to prod still return 400 P0001 from the trigger until migration 009 is applied to prod. |
| May 18 | **Phase D.1 (Blind Draw) shipped to production.** 14 commits to master across the overnight session (started May 17 evening, finished May 18 ~1:30 AM PST + post-launch fixes). 272/272 vitest green across 27 files; end-to-end live-verified on a real 2-team / 3-player round (Team 1: Bill T · Bob B, Team 2: Dan G · Dan S · Dave V; Dan G drawn for Team 1's blind-draw fill, leaderboard rendered correctly with 🎲 caption + "Net −11" inline). **D.1 commits in order:** `f307057` migration + RPC + DB trigger + audit log; `2ea7ce3` data layer (`blindDraws[]` + `droppedAfterHole`); `bf80159` S1 shared ⋯ overflow menu (Mark/Undo Left + Remove consolidated, both admin + scorecard); `ae9cdb3` initial auto-fire RPC trigger + S5 toast (later replaced — see hotfix below); `6413180` S2 pre-fire warning banner; `3cad777` S6 blind-draw rendering in `RoundResultsView` + read-only scorecard; `0cc1592` S7 Individual Rankings exclusion (fills + dropouts); `2779241` WriteQueue P0001 classification + specific stale-failure copy; `50842db` `snapshot-d1.sql` + unit tests for pairing + terminal reason. **Same-night hotfix pivot:** `73de23c` removed auto-fire entirely and replaced with explicit per-team Submit Final Scores button (auto-fire raced A6's "first +/− tap commits par" — player tapping `+` on hole 18 wrote par before they could adjust, completion check passed, RPC fired, round locked); `485dfa0` test refresh (deleted end-round-flow tests, added 6-case submit-flow suite). **Post-launch UX fixes:** `c309e18` fixed missed `isLocked` gate on the `+` button (the earlier `replace_all` only caught `−` due to different surrounding context) + added client-side score-write guard for `submitted_teams` (closes integrity gap between team submit and round-level finalize) + surfaced drawn player's aggregate score (Net/Gross/Pts format-aware) inline on every 🎲 caption (`results.ts` refactored to two passes — precompute `enginePerTeam` map, then assemble TeamRows with cross-team engine lookups). **STATUS.md updates** along the way: `00375bd`, `773a43f`. **Data model adds:** `round_players.dropped_after_hole INTEGER NULL CHECK (1..17)`, new `blind_draws` table (round_id, short_team_number, drawn_player_id, hole_range_start, hole_range_end, random_seed, created_at + index on (round_id, short_team_number)), new `round_player_actions` audit table (mark/undo dropout events, surface = admin\|scorecard), new DB trigger `scores_reject_on_complete` (raises `P0001 round_finalized` on writes against finalized rounds), new RPC `finalize_round_with_blind_draws` (atomic; returns `'not_yet' \| 'already_complete' \| 'finalized' \| 'pool_too_small'`), and a new behavioral field `submitted_teams: number[]` inside `rounds.format_config` JSON (no migration — existing JSONB column). **Phase D.1 items closed:** D1.1, D1.2, D1.3 (merged into D1.1), D1.5, D1.6 (merged into D1.5), D1.7 (NEW pre-fire banner), D1.8 (NEW post-fire toast), D1.9 (NEW result display), D1.10 (NEW client guard). D1.4 parked DORMANT. **Pre-D.1 ship preceded by a full overnight polish + Phase C close session — see the consolidated May 17 entry below.** |
| May 17 (overnight ship — A1 polish + Phase C close + RoundResultsView extraction) | **Big multi-session night. 6 commits to master across PM2–PM5 sub-sessions + the May 17 morning A1.6 commit.** All consolidated here per the May 20 reconciliation pass; the per-PM detail this collapses lived in STATUS.md until the same pass folded it. **Commits in order:** `0639b57` (May 17 morning) — **A1.6** shipped: F9 / B9 / Tot cumulative-net row on the scorecard team-net pill (13px below the headline), helper `getTeamNetDeltaForHoles(holes): number \| null` walks `buildRoundInput("net").perHole`, Stableford collapses to absolute points by the C3 convention; 240/240 tests. `34699b2` (PM) — **A1.7** shipped: tap-to-expand hole-by-hole on each player row of the live scorecard, extracted to new reusable `src/components/scorecard/PlayerHoleGrid.tsx` (props: `scores`, `par`, `currentHoleIndex?`, `showRunningTotal?`) so C PR 3 can consume it unchanged. Multi-expand (others don't auto-collapse), chevron + left-section tap both trigger, +/− stays independent. Confessed deviation: name-span `setRemovePlayerModal` moved into the expand panel as a small underlined link (per spec, tapping the name now expands). 11 new tests using `react-dom/server.renderToString` (no jsdom dep). 251/251. **Phase A.1 fully closed** at this point. `d322a30` (PM2) — **Phase C PR 3 / C4 + C5 + C6** shipped: full rewrite of `/round/[id]/summary` from raw-absolute view to all-teams ranked drill-down with header (date + read-only FormatChip + course + Final/In-progress tag), team cards (gold/navy rank badge, F9/B9 leg row, format-aware big total via `formatTeamTotal`), per-player rows on expand (Gross + Net side-by-side, performance color), and the A1.7 `PlayerHoleGrid` on per-player expand. Multi-expand at both levels. Stableford per-player Net rolls up `result.perHole[].result.perPlayer.points` inline (engine's existing field). **Phase C closed.** `d4cc29a` (PM3) — restored `Individual Rankings` cross-team section below team cards on the summary: flat read-only list of every player across every team, sorted ascending net (best-N) or descending points (Stableford), tie-skip rank semantics matching `rankTeams`. Inlined the sort/rank logic rather than extracting a shared helper (no third caller to justify yet). Uses already-loaded data, zero new queries. `cce58d9` (PM4) — **Shared `<RoundResultsView/>` extraction.** New `src/lib/round/results.ts` (pure async loader + `LoadedRoundResults` types + `LoadRoundResultsOutcome` variants) + new `src/components/round/RoundResultsView.tsx` (visual component, owns multi-expand state via two internal `Set<number>`). Both `/round/[id]/summary` (~770 lines → ~60) and `/leaderboard` (~450 → ~180) now render the shared view. Tapping a team on `/leaderboard` drills in inline (no extra navigation). Confessed trade-offs: per-team `thru N` collapsed to a single round-level header tag; leaderboard date format changed; `/leaderboard` empty states stay leaderboard-local. `370c7df` + `00a54c3` + `23d7379` (PM5) — **Phase A.1 polish, 3 commits:** (P1) drop redundant "Tot" from team-pill F9/B9/Tot row; (P2) replace green-text birdie with traditional concentric-shape notation marks on `PlayerHoleGrid` (circles for under-par, squares for over-par, tiered to triple); (P3) three-row visual hierarchy on hole / par / gross (navy 500 / italic muted / semibold primary). 8 new `PlayerHoleGrid` tests bringing total to 259/259. `6d964b7` — ROADMAP polish section + STATUS PM5 entry. **Side-effect propagation worth noting:** the P2 + P3 changes to `PlayerHoleGrid` propagate to `<RoundResultsView/>` consumers (`/leaderboard` + `/round/[id]/summary`) since they share the component — that's intentional spec-allowed propagation, not a separate edit. All commits `tsc --noEmit` clean. |
| 2026-05-20 (late evening) | Surfaced parked design problem from May 20 admin-edit chat: handicap_index changes retroactively corrupt past rounds' CH via the LT1 self-heal. Real bug, not theoretical — would have bitten by Friday when first post-USGA-update HI change rolls in. Same fix unblocks H.5 historical import (which needs per-round HI for past rounds). Locked design: snapshot HI on round_players, gate self-heal on is_complete, auto-cascade admin edits to active rounds only. Added Phase H.2.5 with 6 sub-items, locked decisions subsection, TD21 doc-cleanup item. Phase H.2.5 inserted into active priority order between Phase H.2 and Phase E. No code changes in this entry — roadmap-only housekeeping. |
| May 13 | **Marathon session — four threads all shipped to master + live in production.** **(1) Sentry phase 1 plumbing** (`47cebc9` + `8042500`): `@sentry/nextjs` 10.53.1, DSN via `NEXT_PUBLIC_SENTRY_DSN` in all three configs, source-map upload via `SENTRY_AUTH_TOKEN` in Vercel, tracing rate 1, session replay off. Phase 1 only — no custom emissions at install time. **(2) Bug 2 dot-rail CSS mitigation** (`5729e2f`): `touchAction: "pan-x"` on the rail + `"manipulation"` on each dot + 35×35 → 44×44 targets (WCAG 2.1 AA). Mitigated, **not yet confirmed-fixed under live conditions** — JS movement-threshold guard queued as follow-up if user testing surfaces continued snap-back. **(3) Bugs 3 + 4 May 11 duplicate-rounds fix** (`df0ee7b`): two `rounds` rows existed for `2026-05-11`. Migrations `005_fix_may11_duplicate_rounds_cleanup` (reparents round 91's `round_players` onto round 90 with team_number shifted by +3, then deletes round 91; idempotent `DO` block raises if counts ≠ 10 / 5 / 180), `006_rounds_played_on_unique` (UNIQUE constraint), `007_rounds_updated_at` (column + BEFORE UPDATE trigger). Code: admin `ensureRoundShell` and `/round/new createRound` both use find-or-create with 23505 fallback. New `initialLoading` gate on the admin format strip buttons. Dropped stale `is_complete = false` filter from `/round/new`'s lookup. **(4) Bug 1 Option 3 — durable write queue, Phases A–E** (`08ecdce` → `116801f` → `cc0d773` → `76e1a8b` → `668da1e`). Five back-to-back PRs, each reviewed on a Vercel preview before rebase-merge. Design doc `59ec72c` at `docs/option-3-write-queue-design.md` captures all 14 locked decisions (D1–D14). Phase A switched `setScore` to `.upsert(... { onConflict: "round_player_id,hole_number" })` — no migration needed because `scores_round_player_id_hole_number_key` already existed in prod (Phase 2's audit had missed it; `list_tables` doesn't surface unique constraints). Phase B shipped `src/lib/writeQueue/` — durable `WriteQueue` class + localStorage storage + backoff helper + 27 unit tests, not yet imported. Phase C wired the queue into the scorecard: `setScore` enqueues, `load()` drains before rehydrate and overlays still-pending items so optimistic state survives offline-on-remount. Phase D added the End-Round reconciliation flow: hail-mary drain → 30s timeout with "Skip and finish" at 15s → first/second-attempt dialogs with `Copy details` clipboard. Bug fix in `drainLoop`: `attemptedInThisPass` set prevents hail-mary infinite-loop on a persistently-failing item. Phase E added the homepage stale-failure prompt: mount-only check, `sessionStorage`-scoped dismissal, `Retry` / `View details` (toggles inline round-date) / `Forget` (DangerModal + `queue.forget(ids, "user_forget_stale")` with Sentry log per D14). **Bug 1 closed** for every reproducible mechanism: write failures mid-flight, tab eviction, offline-on-remount, end-of-round recovery, multi-session recovery. **Phase 0.5 closed:** LT1 self-heal verified-pending; LT2 = Bug 1 closed by Option 3. Test surface on master: **240/240 passing**, `tsc --noEmit` clean, 24 test files. Sentry now emitting from the queue's terminal-failure paths with `reason` discriminator covering all five failure entry points. New TD16 logged: verify Sentry source-map de-minification end-to-end the first time an event lands in production. |

---

*This roadmap is the canonical "what to build" document. Companion file `GOBS_Game_Rules_v1.docx` is the canonical "how scoring works" document. When in doubt about scope, check this file. When in doubt about scoring logic, check that one.*
